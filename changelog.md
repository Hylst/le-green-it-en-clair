# changelog

Je note ici ce qui change, même les petits trucs.

## 17/09/2026 — sweep responsive (nuit)

- signalement : débordements visibles (ex. `/developpement/`). Balayage scripté maison (Chrome headless, 60 routes × 390/768/1024/1440, script dans le dossier temp `sweep/`) : 3 vrais bugs, tous le même pattern (flex sans `min-w-0` ni wrap), corrigés en 5 lignes — cartes `problematiques` (+241 px à la charnière 1024, stat `shrink-0` de 936 px), boutons des fiches (+1 px à 390, `flex-wrap`), nav (gaps resserrés sous `xl`, logo en `nowrap`). Plus : libellé KPI `recyclage` qui ne coupait pas au slash (reformulé « Eurostat et Ecosystem »), logo qui wrappait en police navigateur agrandie.
- faux positifs écartés et notés (ticker, sommaire défilable, `pre` en scroll, barre Radix, tuiles Leaflet, images lazy/Canal-U vérifiées OK au scroll et en 200).
- incident de manip : mon `npm init -y` a pollué `package.json` (`type: commonjs`, build cassé) — restauré via git, build 63/63. Ne plus lancer npm/npx avec le projet en workdir.
- vérifié : tsc 0 (via build), build 63/63, 0 débordement aux 4 largeurs, export.

## 17/09/2026 — repli hors-ligne réparé (nuit)

- signalement : page « Vous êtes hors ligne » affichée sur `/developpement/` avec image cassée + console rouge. Cause : serveur local arrêté (de ma faute, fin de passe e2e) → le service worker a servi le repli depuis son cache, et l'illustration `offline-illustration.webp` n'était pas précachée → carré vide. Le fichier existe bien dans l'export, rien d'autre ne manquait.
- correctif : illustration ajoutée au précache, SW bumpé `v1.4.1`. Validé en conditions réelles (serveur coupé : repli servi avec image 1024 px depuis le cache).
- barre grise à droite : non reproduite à 1920 px sur `/offline/` ni `/developpement/` (débordement 0, captures propres) — ressemble au panneau latéral du navigateur, pas au site. À reconfirmer panneau fermé.
- vérifié : build 63/63.

## 17/09/2026 — passe e2e + vision (nuit)

- passe end-to-end sur l'export servi en local sous `/greenit/` (Chromium) : 63 pages, 3 101 liens internes (0 mort), 91 blocs JSON-LD (0 invalide, BlogPosting sur les 12 articles), 61 titres distincts (1 doublon 404, noindex, écarté), 74 images (0 sans alt), 8 onglets outils chargés, quiz bonus/IA 4/4, quiz Défi (question, timer, correction sourcée), carte Leaflet (10 marqueurs), éCO2mix (live + repli), recherche, Escape, 5 captures dark/light/mobile.
- 3 correctifs : recherche insensible aux accents (`search-dialog` + `ui/command`, « bonus reparation » et « ia generative » trouvent, vérifié), espace manquante après l'infobulle source du hero accueil (`①Découvrez` → `① Découvrez`, seul vrai cas sur 13 fichiers), `recycling-electronics.webp` 166 → 83 Ko (768 px, q70, vignette uniquement).
- faux positifs écartés : prefetch RSC `__PAGE__.txt` en 404 (cosmétique connu), chunks lazy outils, images lazy des fiches (16/16 après scroll), `/offline/` hors sitemap (noindex, normal).
- vérifié : tsc 0, build 63/63, export (0 image > 150 Ko, espace présente).

## 17/09/2026 — article IA générative (soir)

- article 12 « IA générative : ce que dit l'ADEME en 2026 » : décryptage de l'avis du 22/07/2026 lu à la source (485 TWh, ×2 en 2030, ×3,7 France 2035, mix électrique, opacité, AI for green à vérifier), widget `ia-quiz` (4 questions, `aria-live`), 2 liens officiels vérifiés (communiqué + librairie ADEME). Sans visuel pour l'instant (icône de repli). Câblage complet + pont fiche IA, SW `v1.4.0`.
- vérifié : build 63/63, quiz + JSON-LD + ponts dans l'export.

## 17/09/2026 — visuel article bonus (soir)

- `blog-bonus-reparation.webp` (44 Ko, 1376×768, fourni par l'auteur, sans texte ni marque) branché sur l'article + listing. Vérifié dans l'export.

## 17/09/2026 — article bonus réparation (soir)

- article 11 « Bonus réparation : le mode d'emploi » : montants numériques lus dans la grille officielle (smartphone 25 €, PC 50 €, TV 60 €, seuils 100/150 €), éligibilité (hors garantie, panne bloquante, labellisé uniquement), parcours en 3 gestes, widget `bonus-check` (4 questions, `aria-live`), 4 liens officiels vérifiés à la main (grille PDF 04/2026, Ecosystem, économie.gouv.fr 11/09/2026, ADEME). Fourchette 10-65 € confirmée toutes filières (économie.gouv.fr). Sans visuel pour l'instant (icône de repli, à fournir par l'auteur). Câblage complet, SW `v1.3.9`.
- FAQ aides réparation : double porte officielle (annuaire Que faire de mes objets + Épargnons nos ressources, ex-« Longue vie aux objets », redirection vérifiée).
- relecture du 17/09 au soir : 5 correctifs (essential sans « deux conditions », TV = max de la grille EEE, VAE sans déduction sur la majoration, « dommages purement esthétiques » ×3) + plateforme officielle jerepare.fr ajoutée (4 éco-organismes, vérifiée) + deep link ADEME montants/carte (màj 24/08/2026 : 1,5 M de réparations, 63 M€, 6 500 réparateurs).
- maillage : 2 ponts retour (fiche réparer-prolonger, article AGEC).
- vérifié : tsc 0 (via build), build 62/62, article + quiz + JSON-LD + ponts dans l'export.

## 17/09/2026 — retouches bonus réparation / QualiRépar (soir)

- FAQ + 3 articles blog : formulations harmonisées « réparateur labellisé QualiRépar, bonus déduit de la facture » (10 à 65 €, 25 € smartphone), source blog avec millésime (bilan 2025).
- vérifié : build 61/61. Reste le vrai chantier article dédié (cf. suivi-contenu chantier 4, toujours ouvert).

## 17/09/2026 — article RGESN et maillage renforcé (soir)

- article 10 « Le RGESN en 10 minutes » : origine REEN, 78 critères en 8 familles, exemples réels (3.7, 5.7, 6.2), widget `rgesn-check` (4 questions, `aria-live`), visuel `blog-rgesn-10-minutes.webp` (29 Ko), sources et liens officiels. Câblage complet, SW `v1.3.8`.
- maillage : 6 ponts ajoutés (reconditionné vers Back Market, smartphone réparable vers Fairphone, AGEC vers RGESN, fiches écoconception/streaming/télétravail vers RGESN et box).
- vérifié : tsc 0, build 61/61, ponts rendus sur les 5 pages, quiz + image + JSON-LD sur l'article RGESN.

## 17/09/2026 — section livres (soir)

- `/ressources` : nouvelle section « Livres » (ancre `#livres`), 6 livres vérifiés un par un (Bordage ×3 : Buchet-Chastel 2019, Eyrolles 2019, Actes Sud 2021 ; Courboulay, Actes Sud 2021 ; Pitron, Les Liens qui Libèrent 2021 ; Bihouix, Seuil 2014). Liens éditeurs contrôlés (5 en 200, site LLL injoignable : mention « en librairie » sans lien). Résumés maison, pas de copie.
- vérifié : tsc 0, build 60/60, 6 cartes rendues (5 liens + 1 sans lien).

## 17/09/2026 — portraits Fairphone et Envie (soir)

- articles 8 et 9 : Fairphone (2013 Amsterdam, Gen.6 599 €, 12 pièces, 29 kg CO₂e, rapport d'impact 2025) et Envie (1984 Strasbourg, 53 entreprises d'insertion, 215 140 appareils et 206 387 t en 2024, garantie 2 ans), format Back Market avec regard critique, widgets existants réutilisés (quiz réparable, calc reconditionné), visuels auteur (`blog-fairphone-portrait.webp` 39 Ko, `blog-envie-portrait.webp` 80 Ko, sans texte ni marque). Câblage complet, SW `v1.3.7`.
- vérifié : tsc 0, build 60/60, images + widgets + JSON-LD sur les 2 articles, listing, sitemap.

## 17/09/2026 — fiche et article box, fiches enrichies (soir)

- fiche 16 « Box, Wi-Fi et connexion » : 5 sections sourcées Arcep (enquête éd. 2026 sur données 2024 : 3,4 TWh, 9,1 W, ~80 kWh/an ; éd. 2024 : extinction −33 % à 8 h/j), ratio Wi-Fi 4-5× gardé comme ordre de grandeur avec relativisation dans le texte (décision auteur). Visuel `fiche-box-wifi.webp` (25 Ko). Compteurs 15 → 16 fiches, SW `v1.3.6`.
- article 7 « Que consomme vraiment votre box ? » : récit chiffré + widget `box-calc` (curseurs puissance et heures d'extinction, résultats en `aria-live`), visuel `blog-box-conso.webp` (13 Ko), maillage bidirectionnel avec la fiche.
- fiches enrichies : `achat-responsable` (+ section garantie/bonus/seconde main, indice de durabilité 2025) et `datacenters-verts` (+ section 415 TWh AIE 2025, PUE de référence, collecte cloud Arcep 2027), chiffres déjà canoniques sur le site, rien de nouveau non sourcé.
- vérifié : tsc 0, build 58/58, carte box, compteur 16, widget, sitemap 2 URL, 0 « 15 fiches » résiduel.

## 17/09/2026 — portrait, vidéos, vague liens (soir)

- portrait success story : Back Market en 6e article blog (faits publics vérifiés à la main : Paris 2014, 3 fondateurs, 30 M d’appareils dans 17 pays et ~2 Mt CO₂e selon l’entreprise, partenariat Bouygues 2025 ; sans logo, les faits n’exigent pas d’autorisation). Visuel branché le 17/09 (`blog-back-market-portrait.webp`, 39 Ko, vitrine de reconditionné sans texte ni marque, fournie par l’auteur). Câblage complet du premier coup (sitemap, recherche, plan, fil d’Ariane, SW `v1.3.5`).
- vidéos : section dans `/ressources` avec la collection Canal-U « Sobriété numérique » (UVED avec Alt IMPACT, 21 vidéos, mise en ligne le 05/02/2026, vérifiée à la main). 1 vidéo intégrée en lecture au clic via oEmbed officiel (Orgerie, CNRS, 00:08:22, CC BY-NC-SA avec attribution) + 5 cartes avec durée, auteur et lien. YouTube écarté (aucun ID vérifiable à la main). Le 17/09 au soir : les 5 codes oEmbed récupérés un par un (anti-robot contourné avec des pauses, endpoint `oembed?format=json`), les 5 cartes deviennent des lecteurs au clic avec affiches officielles (Mallarino 170270, Da Costa 170242, Ligozat 170206, Ninassi 169767, Le Goff 169827) ; maillage retour ajouté (fiche achat vers portrait Back Market, fiche entreprise vers article audit PME).
- P1 liens : 21 ressources fiches rendues cliquables (16 domaines contrôlés un par un : 200 ou 301 normaux, ademe.fr en 403 anti-bot déjà connu ; entrée « interministeriel » corrigée vers la vraie URL du guide MiNumEco). « GreenFrame, EcoIndex » restés en texte (URL non vérifiée, noté au todo).
- P2 FAQ : 5 liens ciblés (annuaire officiel ×2, `/outils` ×2, `/reglementation`) + 1 cadratin normalisé au passage.
- P4 glossaire : 27 → 32 termes (Réemploi, Bonus réparation, Free cooling, Garantie légale, Taux de collecte) avec liens croisés internes.
- P5 blog : liens de sources vérifiés à la main (librairie ADEME, ODRE, EUR-Lex 2023/1670 et 2024/1799 qui répondent, page À propos Back Market en 200).
- P7 : contrôle trimestriel des liens externes noté au todo.
- vérifié : `tsc` 0, build 56/56, portrait en 200 + sitemap, liens fiches/FAQ/blog/vidéos contrôlés dans le HTML rendu, façade vidéo sans iframe avant clic, `check:typo` sans signalement nouveau.

## 17/09/2026 — visuels blog + fiches, widgets interactifs, petits oublis

- 12 JPEG reçus convertis en webp 70 % 1376×768 (18 à 66 Ko, tous sous 150 Ko) : 6 vignettes fiches (fin des doublons provisoires), hero du blog, 5 vignettes et 5 héros d’articles. Contrôle visuel : charte OK, aucun texte charabia (sélecteur Low/Medium/High lisible sur la fiche streaming).
- 1 widget interactif par article (`components/blog-widgets.tsx`, composants clients isolés) : checklist d’audit en 6 cases avec progression mémorisée, calculateur euros et CO₂e selon le grade, mini simulateur PUE (kW, MWh/an, facture à 0,20 €/kWh), frise AGEC/REEN à feuilleter, mini quiz en 3 questions avec correction immédiate. Résultats en `aria-live`, chiffres déjà sourcés du site, aucun tiret cadratin.
- oublis rattrapés (revue pessimiste) : SW `v1.3.4` (précache + 6 fiches manquantes depuis la vague contenu + 6 URLs blog), print qui déplie les `details` fermés, maillage retour (3 fiches lient leur article de blog), 4 tirets cadratins normalisés dans `eco2mix-live`.
- vérifié : `tsc` 0 erreur (1 corrigée : `ReactNode` dans le hint du calculateur), build 55/55, 12 images en 200, HTML rendu contrôlé (hero, 5 vignettes, 5 widgets, 6 vignettes fiches), `check:typo` sans signalement nouveau (restes = code préexistant).
- revue du 17/09 (soir) : compteur « 8 fiches » périmé sur `/ressources` → 15, 8 onglets outils confirmés, choix « français uniquement » noté dans le todo.

## 17/09/2026 — blog (5 articles) + direct éCO2mix enrichi

- blog : nouvelle rubrique `/blog` (thème orange, `PageHero`, listing + 5 articles en pages serveur avec métadonnées, canonical et JSON-LD `BlogPosting`). Sujets du todo : premier audit PME, reconditionné vs neuf, PUE en 5 min, AGEC/REEN, un an avec un smartphone réparable. Écriture simple, vouvoiement, chiffres déjà canoniques du site avec `SourceTooltip` (75 % ADEME-Arcep 2023, 80 kg et ÷2 ADEME 2025, −75 à −90 % ADEME 2022, PUE 1,56 Uptime 2024, 415 TWh AIE 2025, bonus 4→25 M€ ADEME 2025).
- charge visuelle limitée (règle générale demandée) : « L’essentiel en 30 secondes » en tête, un seul encadré chiffré par section max, approfondissements rangés dans le nouveau `components/more-details.tsx` (`details`/`summary` natifs : lisible sans JS, clavier natif, imprimé ouvert).
- câblage : navigation (Découvrir), sitemap (+6 URLs), recherche (rubrique Blog + 5 articles), plan du site, fil d’Ariane (blog + 5 slugs).
- direct éCO2mix : existait déjà (ODRE temps réel, cache 10 min, fallback de référence), enrichi de 2 blocs dépliables sur `/chiffres` et `/datacenters` : d’où vient la donnée (lien direct ODRE vérifié à la main, quota, mémoire 10 min, mention « référence ») + pourquoi le taux bouge (pointes, hiver, premier levier = durée de vie).
- vérifié : `tsc` 0, build 55/55, 8 URLs en 200, sitemap.xml avec les 6 URLs blog, article PUE au navigateur (détails fermés puis ouverts au clavier et à la souris, liens connexes OK), console = RSC préexistante uniquement.
- reste pour plus tard (images à générer côté utilisateur) : 6 visuels fiches + 5 visuels blog + hero blog, prompts nano banana fournis (fichiers `fiche-*.webp` dans `images/fiches/`, `blog-*.webp` dans `images/blog/`).

## 16/09/2026 — programme contenu (soir) : 6 fiches, chiffres 2026, actu, outil, SEO

- 6 nouvelles fiches (9 → 15) : streaming/gaming, télétravail/visio, e-mails/cloud, objets connectés, impression, enfants/école. Chiffres vérifiés en ligne (Shift 2019+2020+2026, CableLabs 2026, Arcep 2026, SpF Enabee 2025, IoT Analytics 2025, ADEME) ; 2 valeurs non sourcées écartées avant écriture (1,4 Md d'e-mails/jour, 100-200 W consoles). Visuels réutilisés en provisoire (doublons discrets avec pages sœurs), vague 4 bienvenue. Testé : 7×200, 0 image cassée, 15/15 liens (le faux « 9 liens » venait du cache SW).
- chiffres 2026 : OVHcloud corrigé (PUE moyen 1,24 bilan 2025, pas 1,09 ; −73,4 % scopes 1-2, scope 3 = 90 %) + bandeau France (2,7 TWh 2024 Arcep, 10 TWh ADEME, 23-28 TWh 2035 RTE). « À vérifier » levé sur datacenters.
- 8e dossier d'actu : France 2030 EcoIDEN (ADEME, clôture 29/10/2026, 4,4 % / 29,5 Mt / +59 % métaux) + champ `sourceUrl`/`sourceLabel` pour l'attribution externe.
- passe ton : 0 correction (vouvoiement déjà OK, 3 « il faut » impersonnels, 2 « jamais » légitimes — consigne DEEE + distracteur quiz).
- 8e outil : estimateur streaming & visio (Go/mois, kWh/an Arcep, kgCO₂e Base Empreinte 0,0519, équivalence voiture du site). Calculs vérifiés à la main (104/25/1,3 et 225/379/19,6). Virgule décimale FR corrigée. Bug deep-link trouvé et fixé (`#onglet-` vs nom nu).
- SEO : metas déjà OK (19 layouts), FAQ JSON-LD déjà présent. Maillage : agir → fiches, fiche streaming → outil, deep-link `#onglet-*` (useEffect).
- builds 49/49 (fiches) puis 49/49 (outil+SEO), tsc 0. Commits : 54b5ba0, ec89f64, a0af4c3, 8352e2b + SEO/maillage.
- passe pessimiste du soir même : 3 listes en dur oubliées et rattrapées (sitemap.ts + search-dialog + sitemap-page « 8 fiches » → 15), typo « Impact CO2 » visible corrigée, sitemap.xml vérifié à 16 URLs, recherche validée (« Streaming et gaming » trouvé), outil vérifié en dark, 0 débordement à 390 px, console = RSC préexistantes uniquement.

## 16/09/2026 — balayage dark complet (soir) + cleanup écarté

- 7 pages contrôlées pixel par pixel en dark (`motion-off` forcé, état final) : accueil, developpement, reglementation, problematiques (hero + « Solutions à développer » + CTA texture-dots + sources — pleine page trop lourde, 3 timeouts, repli en 3 viewports), chiffres, datacenters, recyclage. Tout lisible, `useChartTheme` fait son job, dégradés et CTA OK, 0 `pageerror` partout.
- `developpement` `dark:bg-slate-950` requalifié : alternance de sections assumée, pas un bug.
- cleanup `website-carbon` gray/slate + `carbon-calculator` blue **écarté assumé** (passe pessimiste, lecture du code avant) : variantes `dark:` déjà complètes, rendu vérifié — uniformiser changerait les teintes pour zéro gain visible, contraire à « modif minimale ».
- `todo.md` : checklist NVDA étoffée (composants phases 1-3 + éCO2mix + quiz, ce qui doit être ignoré).
- warnings console identifiés (bénins) : bannière PWA (pas d'action utilisateur en test) + preload CSS non utilisé à temps (headless lent). `.playwright-mcp/` ignoré par git (vérifié : rien à nettoyer côté dépôt).
- vérifié : aucun changement de code ce soir (docs uniquement), build précédent 43/43 toujours valable, captures supprimées après contrôle.

## 16/09/2026 — axe dark/light (soir) : 8 pages en light + `cas-pratiques` en dark

- balayage light (motion réduite forcée via `localStorage greenit-motion=reduced` : en headless les transitions CSS sont gelées, les screenshots pleine page montraient des sections vides — avec `motion-off` tout est à l'état final) : accueil, developpement, reglementation, problematiques, chiffres, datacenters, cas-pratiques, recyclage — rien de cassé (ticker, compteurs, sommaires, slider, frise, cartes, éCO2mix lisibles).
- `cas-pratiques` en dark : ~40 variantes `dark:` ajoutées (stats emerald/blue/teal, cartes Réparer/Racheter/reconditionné, Améliorations, Cloud/Serveur local, pastilles icônes, hover des cartes de sélection). Raté puis corrigé : la carte Réparer restée claire avec texte clair (fond conteneur oublié → `dark:border-emerald-700 dark:bg-emerald-900/20`, motif repris de `problematiques`) ; 6 doublons `dark:text-slate-400 dark:text-slate-500` (replaceAll `text-slate-400` passé après l'introduction de `dark:text-slate-400` → réduits). Captures dark avant/après conformes.
- `todo.md` : 3 puces mensongères corrigées (contrastes light → fait ce soir ; `text-theme`/`bg-theme` déjà câblés ; « teintes eco2mix changées » → choix initial, rien changé ; accueil `slate-*` → faux positif, variantes déjà présentes ; `- [ ] retester le dark partout` → doublon supprimé).
- vérifié : `tsc` 0 erreur, build 43/43, captures (supprimées après contrôle), 0 `pageerror`.

## 16/09/2026 — phase 3 animations (séparateurs, textures, héros vivants)

- séparateurs (`components/section-divider.tsx`, 0 Ko) : vague SVG teintée `var(--theme-soft)` après le hero sur 6 pages (`comprendre`, `chiffres`, `problematiques`, `mythes`, `agir`, `datacenters`). Accueil exclu (le ticker fait déjà la transition).
- textures (`.texture-dots`) : trame de points en `::before` (z-index négatif + isolation : `background-image` étant déjà pris par le dégradé, une classe utilitaire l'aurait écrasé — vérifié au navigateur : dots + gradient cohabitent) sur 3 CTA (`accueil`, `problematiques`, `developpement`).
- héros vivants : `hero-float` (±6 px, 8 s) sur l'image de `PageHero` (16 pages d'un coup), coupé par `motion-off` + `prefers-reduced-motion` (garde explicite : sinon l'image resterait décalée de 6 px). Héros customs (`accueil`, `comprendre`) exclus : visuels déjà animés par ailleurs.
- vérifié : `tsc` 0 erreur, build OK, dividers présents, float `hero-float` actif / `none` en motion-off, texture au navigateur, captures vague mythes + CTA accueil conformes (subtils), 0 `pageerror`.
- illustrations phase 3 du 16/09 (soir) : 4 JPEG reçus rapprochés par nom, contrôlés visuellement (charte OK, aucun texte incrusté), convertis webp 70 % dans `public/images/` (11 à 16 Ko, 1376×768), branchés avec alts français : `comparaison-duree-vie` (grille 2 col avec le `LifespanSlider`), `quiz-trophee` (résultats du quiz après le score), `carte-france-collecte` (`recyclage` #carte-collecte avant la recherche), `frise-chronologique` (`reglementation` chronologie avant le graphique). Vérifié : build OK, 3/3 visibles au navigateur (captures conformes), `quiz-trophee` dans le chunk quiz (écran résultats, non jouable en test auto), 0 `pageerror`. Faux positif écarté : `carte-france-collecte` non trouvée au 1er check (`currentSrc` vide en lazy sous le pli, visible à la capture).

## 16/09/2026 — phase 2 animations (compteurs, ticker, scrollspy, curseur, progression)

- compteurs (`components/count-up.tsx`) : 11 stats animées à l'apparition (accueil 3 : ~70 Mt, ~80 %, 3,4 % ; `chiffres` 4 : 72,4 Mt, 60 %, 46 %, +114 % ; `datacenters` 4 : 1,5 %, 10 000+, 415 TWh, ~945 TWh). Rendu serveur = valeur finale exacte, pas d'anim si déjà visible ou si animations réduites, formatage fr-FR (U+202F devant % et unités). Exclus : fourchettes (« 2 à 3 ans »).
- ticker (`components/ticker.tsx`, zéro JS) : 6 chiffres sourcés en boucle 45 s sous le hero accueil, lien vers `/chiffres`, 2e moitié `aria-hidden`, pause au survol/focus, statique + scroll horizontal si animations réduites.
- sommaires (`components/sommaire.tsx` + prop `id` sur `Reveal` avec `scroll-mt-32`) : `developpement` (7 ancres), `reglementation` (4 ancres), sticky sous la nav, actif suivi par IntersectionObserver, ancres natives sans JS.
- curseur durée de vie (`components/lifespan-slider.tsx`) : 80 kg CO₂e (ADEME 2025) amortis sur 2 à 6 ans dans le cas smartphone (`cas-pratiques`), input natif (clavier/tactile/lecteurs d'écran via `aria-live`), 40 → 13 kg/an. Pas de 2e instance : `developpement` n'a pas de binaire franc (médias « près de la moitié », vague) — écart au plan assumé.
- progression (`components/reading-progress.tsx`) : 3 px couleur du thème (`bg-theme`, repli primaire) en `scaleX` sur les 5 pages longues (`developpement`, `problematiques`, `reglementation`, `cas-pratiques`, `perspectives`), `aria-hidden`.
- vérifié : `tsc` 0 erreur, build OK (2e essai, 1er échec `spawn` système), 11/11 valeurs finales exactes, ticker `ticker-scroll 45s` + 12 items, scrollspy 7 + 4 liens (actif « Médias »/« Données » selon cadrage, ancre `#ressources` OK), slider 6 ans → « ≈ 13 kg CO₂e par an », progression `scaleX ≈ 0,53` à mi-page, `motion-off` → compteurs figés + ticker statique, 0 `pageerror`, captures ticker/sommaire/slider conformes. Ratés corrigés : `id` insérés dans le `className` par le script (7 + 3 lignes, guillemet manquant — tsc a bloqué), ticker empilé (`block` Tailwind écrasant `display:flex` de la couche components), faux positif `anim:none` (CSS en cache après unregister SW).

## 16/09/2026 — phase 1 animations (niveau « expressif mais maîtrisé »)

- `components/reveal.tsx` : wrapper client autour de `useReveal` (rend `section`/`div`, classes passées telles quelles, visible sans JS et si animations réduites).
- reveal branché sur 23 sections de contenu : `developpement` (10), `reglementation` (4), `problematiques` (9). Exclus : héros + 1ères sections (risque de flash : `useEffect` pose `reveal-init` après le 1er paint), bloc sources `problematiques`, et `guide` (pas de `<section>`, tout dans le 1er écran — écart au plan assumé).
- accueil : `icon-shift` sur les 12 liens/boutons à flèche, `active:scale-[0.98]` sur les 5 CTA (hero + final).
- feedback « Copié ! » (icône `Check` + retour à « Partager » après 2 s) sur les 3 boutons Partager en fallback clipboard : `carbon-calculator`, `website-carbon-calculator`, `sheet-content` (qui gagne son `useState`).
- vérifié : `tsc` 0 erreur, build OK, 10/10 + 4/4 + 9/9 reveals posés et visibles après scroll, `motion-off` → 0 `reveal-init`, feedback « Copié ! » testé (branche fallback forcée : `navigator.share` existe en headless Chromium), 0 `pageerror`, captures developpement/accueil conformes. Faux positifs écartés : SW servant l'ancien build (désinscription → 10 reveals), 404 RSC `__next.*__PAGE__.txt` au prefetch (préexistantes, page non touchée `mentions-legales` aussi, liées au serveur statique — à étudier, pas touché).
- vérifié (audit du 16/09 avant phase 2) : balises équilibrées 10/10-4/4-9/9, sections restantes = les bonnes (héros + 1ères sections + sources `problematiques`), 12 `icon-shift` sur parents (0 sur flèches), 5 `active:scale`, `prefers-reduced-motion` émulé → 0 `reveal-init` et contenu visible, 3e feedback (`website-carbon-calculator`) testé au clic (« Copié ! »), aucun résidu (captures supprimées, scripts dans `/tmp`, serveur de test laissé vivant pour la phase 2).
- vérifié (audit du 16/09 avant phase 3) : pas de résidus ni console.log, mobile 390 px conforme (ticker lisible, sommaire scrollable, slider utilisable), clavier (2× → → 3 à 5 ans, « ≈ 16 kg »), `prefers-reduced-motion` émulé → ticker statique + scroll horizontal + compteurs figés, 0 `pageerror`.
- reliquats repérés (non touchés, modif minimale) : couleurs en dur restantes (accueil `slate-*`, `problematiques` dégradés blue/purple/pink + emerald→teal, `website-carbon-calculator` gray/slate, `carbon-calculator` blue, `developpement` `dark:bg-slate-950`).

## 16/09/2026 — plan design : vagues A à E implémentées

- fondations (A) : tokens `--warning` (l'encart `a-propos` prend enfin sa couleur), 9 `data-theme` + utilitaires (`bg-theme`, `text-theme`, `bg-theme-soft`, `text-theme-ink`, `border-theme`), 5 effets (`lift`, `glow-theme`, `icon-shift`, `link-slide`, `reveal`), option « Réduire les animations » (`MotionToggle` footer, `localStorage greenit-motion`, anti-flash dans le layout), `lib/chart-theme.ts` (graphiques suivent le dark), `lib/motion.tsx` (`useReveal`, visible sans JS), 4 illustrations rapatriées dans `public/images/`, règles design ajoutées à `AGENTS.md`.
- chrome (B) : `PageHero` partagé (badge, titre, intro, image, actions, serveur), navigation/footer/breadcrumb/`theme-toggle` vers tokens, bandeau footer en `warning`, `link-slide` sur les liens, clé `greenit-motion` en mentions légales.
- héros (C) : 16 héros migrés vers `PageHero` + `data-theme` (emerald/teal/red/amber/blue/cyan/violet/indigo selon rubrique), textes à l'octet près, badges d'origine restaurés (icon/label rendus optionnels).
- pages (D) : 0 hex restant dans 12 fichiers (Recharts via `chart-theme`, tooltips en `var(--card)`), tokens mécaniques, `lift` sur les cartes sans hover, `dark:` complétés (`perspectives`, `datacenters`, quiz), `gray` → tokens, `✓` → `Check` lucide, loadings en tokens.
- finitions (E) : drapeaux/⚠/✓/🇫🇷 → texte ou lucide (`visual-analogy` prend des icônes + variantes dark), contrôles natifs teintés (`accent-color`), `quality` 85 partout, `useState("python")` mort supprimé.
- vérifié : `tsc` 0 erreur, build OK, utilitaires présents dans l'export CSS, 0 `pageerror` (accueil, problematiques, outils, par-ou-commencer), captures dark + light du hero rouge, onglets/quiz/toggle motion testés au navigateur, grep : 0 emoji, 0 `gray-` outils, 0 hex graphiques.
- reste : re-tester les contrastes light.
- illustrations du 16/09 (soir) : 18 JPEG reçus rapprochés par nom (confiance au nommage, vérifié aux captures), convertis webp 70 % dans `public/images/` (8 à 70 Ko, tous < 150 Ko, 1376×768 sauf offline 1024²), branchés en prop `image` de `PageHero` (15 héros + alts français), hero `outils` (dans le bandeau sombre) et `recyclage` (migré vers `PageHero` au passage), `offline` + `404` (illustration partagée), dimensions `PageHero` alignées sur les fichiers. Vérifié : build OK, 22 pages balayées au navigateur (0 `pageerror`, 0 image cassée — 1 faux positif lazy sur l'infographie `comprendre`), captures mythes/agir conformes (pas de texte incrusté).
- passe pessimiste du 16/09 (après implémentation) : `par-ou-commencer` migré vers tokens + `icon-shift` sur les boutons Commencer, import `Badge` mort retiré (`guide`), 3 inexactitudes de l'audit corrigées (3 → 4 images rapatriées, `quality` 90 aussi sur `comprendre`, classes dynamiques aussi dans `comprendre`/`enterprise-simulator`), angle mort comblé (§1.7 : 7 pages détail modèles, recherche, loadings), serveur de test relancé (le `nohup` n'avait pas survécu — la page hors-ligne servie par le SW a trahi la panne, bel effet de bord : le fallback offline fonctionne). Reliquats notés dans `todo.md` (effets/lib en attente d'usage, `CHART_FALLBACKS` statiques, teintes eco2mix légèrement changées).

## 16/09/2026 — fix mise en page /par-ou-commencer sur le serveur de test

- cause : pas la page (export nickel), mais `scripts/greenit-server.js` qui ne gérait pas le `basePath /greenit` — toute URL `/greenit/...` retombait sur l'accueil, et les JS/CSS servaient du HTML (« Unexpected token '<' »).
- serveur corrigé : préfixe `/greenit` retiré avant résolution dans `out/`, dossiers → `index.html`, `page` → `page/index.html`, vrai 404 (`404.html`), anti directory-traversal, `out/` par défaut corrigé (`../out`, plus `express` inutilisé qui faisait planter sans la dépendance).
- effet de bord trouvé au passage : le SW avait mis en cache ce HTML sous des URLs de JS/CSS (cache-first sans contrôle), d'où une page toujours cassée même après le fix serveur. `public/sw.js` bumpé `v1.3.3` (l'ancien cache se purge tout seul à l'activation) + garde : on ne met en cache un script/style que si son `content-type` est bien JS/CSS.
- vérifié : build OK, `/greenit/par-ou-commencer/` titre + H1 bons, 0 erreur console, cache SW propre, clic profil Citoyen → parcours affiché, capture d'écran conforme (dark, cartes, fil d'Ariane).

## 16/09/2026 — veille RSS sur /actualites

- veille RSS (chantier `todo.md` soldé, après l'échec de l'ancien `flux-rss` retiré le 13/09) : nouvel onglet Veille d'`/actualites` avec « Dernières nouvelles » chargées à la demande côté navigateur (`components/rss-feed.tsx`). 8 flux en liste blanche, FR d'abord (GreenIT.fr, Institut du Numérique Responsable, The Shift Project, Next.ink, Numerama) puis EN (Data Center Dynamics, The Register, UNEP) ; pastilles activables une par une, statut par flux (chargement / OK / erreur), tri du plus récent au plus ancien, dédoublonnage, max 40 titres.
- honnêteté et droits : uniquement titre + média + date + lien vers l'article d'origine (jamais de copie du contenu), mention « les contenus restent chez leurs auteurs », note dans `mentions-legales` (passage par le service tiers rss2json.com quand le flux n'autorise pas la lecture directe, clé `greenit-selected-feeds-v1` documentée). Écartés après vérification : ADEME (flux vide côté site, bloqué côté proxy), Arcep et AIE (pas de flux), actu-environnement (404).
- technique & sécurité : lecture directe si CORS (Next.ink), sinon rss2json.com (seul proxy encore debout) ; validation stricte des URLs (`https://`), décodage HTML sécurisé via `DOMParser`, timeout réseau 10 s (`AbortSignal.timeout`), cache `sessionStorage` (15 min) évitant les requêtes redondantes, persistance du choix des flux via `localStorage`, onglet `forceMount` prérendu dans le HTML statique (SEO / no-JS).
- a11y, UX & PWA : badges de fraîcheur (`< 24 h` émeraude, `< 3 j` bleu ciel), détection explicite du mode hors-ligne (`WifiOff` + message adapté + actualisation désactivée), annonces `sr-only` pour les statuts de flux, `aria-live` dynamique sur le nombre d'articles, meta description actualités et entrée recherche `search-dialog` enrichies.
- vérifié : `tsc` 0 erreur, build OK, axe-core dark **0 violation** (light : contrastes badges validés), 0 débordement à 320 et 390 px, flux réels affichés (40 titres), tests automatisés Playwright (offline, cache, persistance, cross-browser Firefox / WebKit).

## 15/09/2026 (nuit) — audit contenu : corrections

- contenu : portable 156 kg (fausse part « ~39 kg » retirée), simulateur PUE corrigé (`IT × (PUE−1)` — 25 kW et non 17), série pays de `problematiques` alignée sur Eurostat/Ecosystem 2024 (`chiffres`), indice de durabilité France vs étiquette UE 2023/1669 vs 2023/1670 (pièces 7 ans, màj OS 5 ans), directive 2024/1799 (+12 mois après réparation), `mythes` 75-90 %, mini-quiz accueil sourcé, cycle de vie `comprendre` dédoublonné (12 000 L / 250 kWh = fabrication, extraction incluse), `perspectives` recadrée « projection illustrative », Project Natick arrêté en 2024.
- outils : scénario de base sans économies négatives (préventif +20 % réservé aux scénarios optimisés), hypothèses affichées, simulateur sobriété sans cumul réparation/reconditionné, `it-audit` durées 5 ans + parc vide neutre, champ poids `2.1` (virgule invalide en `type=number`), barème A+ labellisé, repère 330 kg par internaute (EENM 2025 : 1,8 Gt ÷ 5,35 Md), quiz : « questions répondues » corrigé, sources Q74/Q94/Q99 honnêtes, `QUIZ_CONTENT_VERSION = 2`.
- a11y/PWA : safelist Tailwind (`@source inline`) pour les classes dynamiques (rose, pink, slate dark, gray… jamais générées avant — cartes invisibles), bouton « Installer l'app » visible sur mobile, `apple-icon.png` + `icon-512-maskable.png`, manifest `id`/`shortcuts`, SW `v1.3.1` (fiches + modèles précachés), nginx (`sw.js` no-cache, vrai 404, `error_page` sous `/greenit`), twitter par page (fallback OG), ± nommés, cartes cliquables au clavier, titres `reglementation` h2→h3→h4, « Fermer ».
- éditorial : CTA `agir` honnête (« Guide du recyclage »), FAQ (conseil achat 2025 + bloc Sources), sources `recyclage` 2024, durées `politique-numerique` 5/5-7 ans, `charte` « Objectif (3 ans) », sommaire `tableau-bord`, ressource ADEME, glossaire 20 termes ancrés (+ indice de durabilité), mentions légales (repo public), footer sourcé + lien `/guide`, section vide de `developpement` virée, `npm run check:typo` créé.
- vérification manuelle des données (commit `2c0021f`) : −87 % résiduel retiré, quiz Q70 corrigé (étiquette = 2023/1669, pièces = 2023/1670), mythes 75 % attribué à ADEME-Arcep 2023 (et non 2026), 10 % d'électricité mondiale non sourcé retiré, collecte vs recyclage clarifiés (46 %, 22,3 %), fourchette « −91 % » et durée « 5 à 10 ans » inventées retirées, PUE 1,0/1,2 en virgule, première passe « % » sur les pages de contenu.
- typo (commit `937209e`) : script de contrôle (espace uniquement, diff validé ligne à ligne), tous les « % » du texte visible (y compris options de quiz et libellés), coquilles « filière agréée », « Mise à jour », « avant d'envisager », logs console SW/installer supprimés.
- mobile (commit `1068cea`) : 0 débordement horizontal sur les 30 routes à 320, 390 et 768 px — tableaux dans `overflow-x-auto`, en-têtes et fil d'Ariane qui passent à la ligne, pas d'étapes réduit et emojis d'analogie adaptés, cartes `min-w-0` ; quiz « 467 fois » et explication Q76 (MIPS) reformulée.
- ADEME 2025 (commit `609e714`) : migration des facteurs appareils vers l'outil officiel Impact CO₂ (mise à jour 2025) — smartphone 80 kg, tablette 87, portable 193, fixe 259 (pro), écran 93, TV 370 ; fabrication à 99 % du carbone pour un smartphone, « 20 % » requalifié « tous indicateurs » ; outils, pages, modèles et quiz alignés.
- hygiène (commit `24b38a8`) : 34 composants `ui/` + 2 hooks shadcn jamais importés supprimés (49 → 15 fichiers) ; emojis des titres, boutons et libellés remplacés par des icônes lucide (emojis pédagogiques conservés) ; icônes appareils, statuts cloud et catégories recyclage passés en lucide.
- a11y (commit `b411973`) : axe-core (WCAG 2.0/2.1 A/AA) sur les 31 routes en dark → **0 violation**. Correction des fonds clairs sans variante dark, badges passés de 600 à 700, textes sur fonds sombres, curseur `cas-pratiques` nommé, SVG langages en `role=group`, barres de comparaison assombries ; mobile 390 non régressé.
- contenu (commit `64cf9aa`) : nouvelle fiche pratique « IA générative » (sources ADEME, avis juillet 2026, AIE, RGESN/AFNOR) branchée partout (hub, recherche, sitemaps, précache SW v1.3.2) ; FAQ +3 questions (27) ; glossaire +7 termes (27, avec MIPS, RGESN, GR491, IA frugale, étiquette énergie UE, CSRD, obsolescence logicielle) ; 7ᵉ dossier d'actualités sur l'avis ADEME IA 2026.
- vérifié : `tsc` 0 erreur, build 42 pages, classes CSS présentes dans l'export, manifest JSON valide.
- reste (dans `todo.md`) : emojis UI, 30 fichiers `ui/` orphelins à trancher, relecture question par question du quiz, lecteur d'écran réel, et une question de fond : migrer ou non les facteurs appareils vers l'ADEME 2025 (Impact CO₂ : 80 kg smartphone, 193 kg portable…).
- vérifications en ligne du 15/09 au soir : ×3 ADEME-Arcep confirmé (communiqué ministères 07/03/2023), GR491 déplacé vers `gr491.isit-europe.org` (fiche corrigée), « 70 kg » confirmé par le SDES (infographie 11/04/2025), question MIPS du quiz reformulée (le « 200 kg » n'était pas sourcé), liens de l'export contrôlés (1 935 liens internes, 0 cassé ; 29 externes, seuls des 403 anti-bot).

## pas encore sorti — septembre 2026 (13 au 15/09)

- fix du 14/09, build qui passe (42 pages) :
  - pwa `/greenit` : manifest (`start_url`/`scope` + icônes `.webp`), sw (chemins + fallback), layout (`register('/greenit/sw.js')`, OG en `/greenit/...`, `generator: Next.js`), leaflet (`/greenit/images/...`)
  - refs mortes : fallback placeholder -> vraie image, breadcrumb sans `flux-rss`, `font-heading` -> `font-poppins` x3, sitemap (+offline + 7 modeles, date en dur + `force-static`), dockerfile (`npm ci`, plus de pnpm)
  - outils : boutons télécharger -> `window.print()`, audit -> son `exportPDF`, partager -> `navigator.share` + presse-papier, vieux `GreenITQuiz` viré, recherche complétée (39 entrées fin 15/09)
  - build : `sheet-content.tsx` avait une `</div>` en trop qui plantait tout, réparé
  - trouvé au passage : `npm run lint` marchait pas (eslint pas installé) -> script viré le 14/09, le check c'est la CI (`npm run build`). Reste : installer eslint + nettoyer les erreurs `tsc`.
  - perf `/outils` le 14/09 : découpé en 6 modules (`components/outils/`) en `dynamic` + `ssr: false`, la page ne charge chaque outil qu'à l'onglet.
  - PWA le 14/09 : icônes 192/512, manifest complété, SW `v1.2.0` (depuis bumpé `v1.3.0` pour le précache offline). CI GitHub Actions ajoutée (build à chaque push).
  - re-vérif pessimiste : zéro résidu (`placeholder.svg`, `flux-rss`, `font-heading`, `v0.app`, `GreenITQuiz`...), sitemap exporté vérifié (`offline` + 7 modeles dedans), import `Download` inutilisé viré de `sheet-content`.
  - gitignore : viré les entrées `gemini.md` / `cursor.md` (ces fichiers existent pas, et pour pas dupliquer : tout est dans `agents.md`, valable tous assistants).
  - accessibilité le 14/09 : lien "Aller au contenu" + `id` sur `main`, focus visible renforcé, onglets outils en `tablist/tab/tabpanel` + `aria-selected`, mini-quiz en `aria-pressed`.
  - sécu le 14/09 : headers nginx (`Referrer-Policy`, `Permissions-Policy`, viré `X-XSS-Protection` déprécié), `rel="noopener"` partout (manquait que sur actualites), 7 dépendances mortes virées (`@vercel/analytics`, `date-fns`, `embla`, `input-otp`, `vaul`, `react-day-picker`, `sonner`) + 7 composants shadcn orphelins supprimés (dont `sidebar` et son cookie mort).
  - RGPD le 14/09 : mentions légales réécrites pour de vrai (zéro cookie, thème en localStorage, cache SW, tuiles OSM, pas d'analytics). Contenu : section recyclage ajoutée au guide.
  - audit navigateur le 14/09 (vrai Chromium sur l'export prod) :
  - images cassées en prod : 24 chemins sans `/greenit` (hero, fiches, comprendre, développement, ressources) -> préfixés, vérifié visuellement. C'était le même nid à bugs que le basePath.
  - console propre : manifest (`purpose` invalide viré), favicons déclarés dans le layout (plus de 404 `/favicon.ico`), `qualities: [75,85]` dans la config, meta `mobile-web-app-capable` ajouté.
  - PDF orphelin (`guide-recyclage-green-it.pdf`, lié nulle part) -> bouton téléchargement dans le guide (et ça a viré un import `Download` mort).
  - 31 boutons-dans-des-liens corrigés (`Button asChild`, comme la FAQ le faisait déjà) : lecteurs d'écran OK.
  - recherche 100% français (les textes par défaut anglais de shadcn), onglets outils navigables aux flèches (motif APG : ids, roving tabindex, panneau lié).
  - SW : les pages sont en network-first (v1.2.1, depuis bumpé v1.3.0). Avant, un visiteur qui revenait après un déploiement gardait le vieux HTML qui pointait vers des JS effacés -> site mort. Testé en reproduisant le bug en local.
  - testé au clavier : skip-link visible au 1er Tab, menus + Escape, flèches dans les onglets. Reste du bruit console (prefetch Next `_rsc` en 404 sur l'export statique) : cosmétique, la navigation marche, noté dans `todo.md`.
  - suggestions implémentées le 14/09 :
  - titres uniques par page : template `%s | Le Green IT en clair` + 19 mini-layouts (les pages sont en `"use client"`, donc pas de metadata directe possible) + 4 pages serveur en direct. Vérifié dans l'export (`Outils interactifs | Le Green IT en clair`...).
  - page 404 (`not-found.tsx`, génère `404.html`) + `error_page 404 /greenit/404.html` dans nginx. Au passage : le fil d'Ariane affichait `_not found` au prérendu -> erreur d'hydratation React #418, réparé avec `suppressHydrationWarning` sur le segment.
  - image OG 1200x630 générée avec Agnes (5 essais : le modèle massacre le texte français, version sans texte adoptée après contrôle visuel) -> `public/og-cover.png`, URLs absolues dans le layout.
  - screenshots PWA (accueil desktop 1280 + outils mobile 390, vraies captures navigateur) déclarés dans le manifest.
  - print CSS : nav + footer masqués à l'impression (pour le bouton "Imprimer le guide"), fond blanc forcé. Emoji 🎯 du titre masqué aux lecteurs d'écran.
  - fil d'Ariane : labels `guide`/`mentions-legales`/`offline` ajoutés + `aria-label` en français.
- `website-carbon` honnête le 14/09 : les résultats simulés (`Math.random()`, faux « SWD 2025 / 0,81 g », scores Lighthouse inventés, hébergeur vert à pile ou face, boutons PDF/partager morts) sont remplacés par un estimateur transparent — poids saisi par l'utilisateur (repère HTTP Archive), green-check en direct via l'API publique Green Web Foundation (repli manuel si KO), calcul local SWD v4 affiché (0,194 kWh/Go AIE × 494 gCO2e/kWh Ember 2023, −24,3 % si vert, hors fabrication des équipements), équivalences recalculées sur la vraie valeur avec facteurs affichés, boutons imprimer (`window.print()`) + partager (`navigator.share` + presse-papier) branchés, liens vers Website Carbon / EcoIndex / PageSpeed / SWD. Vérifié : build ok, `tsc` propre sur le fichier, export `/greenit` contient le nouveau composant (plus de `mockResults`).
- `actualites` assainie le 14/09 : fini les fausses dépêches attribuées à de vrais organismes (ADEME, Commission européenne, Shift...) et le faux « flux en direct » (pastille pulsante, `setTimeout`, bouton Actualiser bidon). Les 6 encarts « À la une » sont des exemples assumés (badge Exemple + « la rédaction », sujets réécrits sur des faits vrais avec liens vers nos pages), l'onglet Veille est un annuaire de 5 sources externes réelles sans dates ni titres inventés. Vérifié : build ok, `tsc` propre, export sans « Flux en direct ».
- `problematiques` corrigée le 15/09 (chiffres P0) : e-déchets sur trajectoire GEM 2024 (62 Mt 2022, ~69-70 Mt 2025, 82 Mt 2030), recyclage monde 22,3 %, mythe des 80 % exportés remplacé par export illégal minoritaire chiffré (Nigeria ~288 kt, Ghana ~215 kt), IA réécrite (~0,3 Wh en texte simple), SD ~0,7 Go/h (Kamiya), lithium ×3 entre 2017 et 2022 (AIE), renouvellements 62 % (ADEME 2026, au lieu de 88 %), UE 5 ans de màj + 7 ans de pièces (2023/1670), eau 12 000 L, TSMC ~67 % (Counterpoint T4 2024). Stabilisation manuelle après interruption d'un sous-agent (son diff repris et durci, rien inventé). Vérifié : `tsc` propre, build ok, export contrôlé (zéro ancienne valeur).
- `datacenters` + `cas-pratiques` corrigés à la main le 15/09 (chiffres P0) : 415 TWh et 1,5 % (AIE 2024), PUE 1,56 monde / 1,45 Europe (Uptime 2024), camembert 64/28/8 avec point clé réécrit, portable = Paris-Marseille aller-retour (DGAC, au lieu de Paris-New York), 800 kWh ≈ 2 mois de foyer, serveurs 3-5 ans (le bâtiment 10-15 ans), RAM upgradable nuancée, CTA `datacenters` branchés (`/agir`, `/outils` en `asChild`), section Sources de `cas-pratiques` remplie. Vérifié : `tsc` propre, build ok, export contrôlé.
- `faq` + `reglementation` corrigés à la main le 15/09 (droit P0) : CSRD réécrite (Omnibus I : >1 000 salariés ET >450 M€, ~5 000 entreprises, PME exclues, transposition 19/03/2027), réparation applicable depuis le 31/07/2026 (+12 mois de garantie, prix raisonnables, plateforme 2027), bonus 10-65 € (smartphone 25 €, QualiRépar), garantie légale 2 ans (24 mois présomption), indice durabilité réécrit (2 notes, TV/lave-linge 2025, étiquette UE smartphones 20/06/2025), DEEE en réexamen, lignes REEN non vérifiées retirées, 85 % recyclage / 65 % 2025 / 1,76 g / Python 57x / 2,7 Mt / box 65 kWh / Wi-Fi 20x / apps arrière-plan / 33 mails corrigés ou retirés, label NR ~350, Energy Star sans nombre, conformité marquée illustrative. Vérifié : `tsc` propre, build ok, export contrôlé (zéro ancienne valeur).
- `agir` + `guide` + `developpement` corrigés à la main le 15/09 : conseils faux retirés/nuancés (fermeture apps arrière-plan, Wi-Fi 20x → ~4-5x, box 10 € → ~5 €/an, heures creuses et pic 18h-20h reformulés, Lilo retiré d'Ecosia, durées harmonisées 5 ans, onglets/favoris/mode sombre reclassés en bonus), code `developpement` assaini (Prisma `select` valide, O(n²) 1 000x, WebP ~30 %, autoplay/GraphQL/serverless nuancés, exemple marqué illustratif, source Pereira 2017). Préalable : balayage non-régression complet sur les fichiers déjà traités (aucun oubli, 2 points déjà parqués). Vérifié : `tsc` propre, build ok, export contrôlé.
- Boutons morts branchés à la main le 15/09 : CTA `agir` → `/outils` + `/recyclage`, `recyclage` → scroll vers la carte (`#carte-collecte`) + PDF recyclage, `perspectives` → `/agir` + `/outils`, Exporter `chiffres` → Imprimer (`window.print()`), certificat quiz → Imprimer, ancres `par-ou-commencer` sans fragment mort, bouton ZIP `modeles` retiré (aucun ZIP existant). PDF `guide-recyclage-green-it.pdf` régénéré depuis le script corrigé : 2 kg ≈ 17 km (fini Paris-Istanbul), hiérarchie réduite > réparer > réemployer > recycler, 3-4 % sourcé. Vérifié : `tsc` propre, build ok, export contrôlé (zéro `alert`, liens et PDF présents).
- Soldes des oublis P0 le 15/09 (phase A du plan, commits `bcb14cc`, `042326a`, `78398f7`, `67d4bde`, `32d104b`, `38850ee`, `97701e5`, `dc7468c`) : accueil (74,7 Mt → ~70 Mt GEM 2024 ; 78 % → ~80 % ADEME-Arcep 2023 ; 4 % → 3,4 %/1,8 Gt EENM 2025 ; 2,3 ans → 2-3 ans ADEME 2026), `mythes` (22,3 % de collecte/recyclage en 2022, Wi-Fi ~4-5x, sources ADEME 2022/2026, GEM 2024, Kamiya 2020, chiffres non sourcés retirés), `fiches-pratiques` (box ~26 kWh/an Arcep 2026, Python ~76x Pereira 2017, recyclage ~79 % Ecosystem 2024, ~2 kg par smartphone, garantie légale 2 ans, `quefairedemesdechets.ademe.fr`, dates 2026), durées harmonisées 2-3 ans, arbres 2,5, sources précises dans les 4 outils, badges 2026, recherche complétée (39 entrées : 8 fiches, 7 modèles, offline, mentions légales), docs locales + suivies mises à jour. Vérifié : `tsc`, build, export sans ancienne valeur.
- Quiz corrigé le 15/09 (commits `4f032b0` à `00db832`) : les 100 questions repassées une par une (22,3 % de collecte/recyclage GEM 2022, 17 terres rares vs ~50 métaux, durée de vie 2-3 ans, 3,4 %/1,8 Gt, box 50-100 kWh Arcep 2026, pièces 7 ans UE 2023/1670, AGEC 2020, PUE 1,56/1,45 Uptime 2024, 415 TWh/1,5 % IEA 2025, bots 51 % Imperva 2025, dark mode Purdue 2021, BYOD redéfini, sources fictives virées, chiffres non sourcés transformés en questions conceptuelles). Moteur : niveau calculé sur la somme réelle des points, garde division par zéro (« — »), tirage Fisher-Yates, timer du mode Défi qui continue pendant l'explication, attestation imprimable à partir de 60 % (nom facultatif, date, mode, score, niveau, impression dédiée `printing-certificate`), réponses en `radiogroup`/`radio` avec libellés sr-only et explication `aria-live`. Vérifié : `tsc`, build, export, test navigateur sous `/greenit/` (mode Découverte 35/140 = 25 %, score moyen 4 points, bouton « Imprimer mon résultat » à 25 %, CSS d'impression présent ; console = prefetch `_rsc` connu).
- Vague B contenu + images le 15/09 (commits `4fd4fd9` à `0391479`) : contenu faux/trompeur corrigé (`perspectives` ×3 d'ici 2050/0,3 Wh/cloud mutualisé, `guide-sensibilisation` 3,4 %/100 M tiroirs/75 %, `charte` 3,4 %/1,8 Gt, `comprendre` 250 kWh en 2-3 semaines/Paris-Marseille, 18 Go Arcep 2025, 22 % GEM, 50 kg/12 000 L, `problematiques` graph 2010 retiré, France 46 %, 79/16/5 ADEME-Arcep 2023, `chiffres` tablette 63, 1,6 Mt/24 kg, `carbon-calculator` Paris-Marseille AR, `growth-animation` linéaire 33,8 + 2,4/an, `sobriety-simulator` mails ~1 %, `cloud-comparator` AWS 2023/PUE légende/scores indicatifs, `enterprise-simulator` 0,20 €/kWh, 60 €/an, 22 kg, fiches bonus REP/75 %/−30 %/Fairphone/Samsung, `a-propos` 3,4 %, ×3 2050, © 2026). Images : `public/img_ori_non_opti/` (35 fichiers, 6,7 Mo) sorti du build et conservé en local (`img_ori_non_opti/`, gitignoré), 18 fichiers orphelins supprimés (dont 3 icônes leaflet webp), `sustainable-coding` passé en webp (95 Ko au lieu de 883 Ko), `og-cover` recompressé en jpg 1200x630 (94 Ko au lieu de 1,04 Mo), screenshots PWA en webp (manifest mis à jour), `co2-distribution.webp` (571 Ko, contradictoire) et `lifecycle-infographic.webp` (charabia) supprimés, entrées `ressources` rebranchées vers `/comprendre` et `/chiffres`. Vérifié : `tsc`, build, export sans référence morte, test navigateur (`perspectives`, `ressources`), `out/` passé d'environ 21 à 13 Mo.
- Périmètre C le 15/09 (commits `0ea3963` à `1828bb6`) : bloc Sources ajouté aux 8 fiches (champ `sources[]` + carte dans `sheet-content`), `sitemap.ts` complété (8 fiches détail) et `sitemap-page` complété (actualites, faq, mythes, par-ou-commencer, fiches-pratiques, guide, modeles, mentions-legales, offline), filtres par catégorie de `modeles` branchés (8→1→8 testé), formats annoncés corrigés (PDF impression, plus de DOCX/XLSX/PPTX fantômes), grille d'audit ramenée à ses 26 vrais critères, années du plan DSI passées en relatif, RAM `politique-numerique` à 8 Go, chiffres harmonisés (PC fixe 169 kg, écran 248 kg, smartphone 50 kg, reconditionné −75 %, cas-pratiques 88 %→62 % et « 1 an de plus ≈ un tiers », sources `cas-pratiques` complétées), cahier des charges/guide sensibilisation/tableau de bord sourcés et datés en relatif. Docs : bandeau de statut dans `audit-contenu-2026-09.md`, `.gitignore` `audit-*.md` retiré (le fichier est suivi). Vérifié : `tsc`, build, export (sitemap 9 URLs fiches, bloc Sources présent, 26 critères, 0 « 4 Go RAM »), navigateur (filtres modèles, Sources fiche).
- Passe exhaustive du 15/09 (zones jamais auditées) : composants — classes Tailwind dynamiques qui ne s'affichaient jamais corrigées (scale-comparison, visual-analogy, en-têtes du guide), liens des ressources des fiches réparés (labels + URLs, domaines nus liés), garde de maxValue du comparateur, chronologie linéaire 2,6 Mt/an, barres d'impact en tokens sombres + source, zoom d'image accessible au clavier (bouton, qualité 85), carte Leaflet : setView dans un effet, SVG cliquables au clavier, logs PWA en français ; système — 404 et hors-ligne en noindex (fin du conflit index/follow), hors-ligne retirée du sitemap, lien Next.inpact mis à jour, plan du site complété (les 8 fiches et 7 modèles), tooltips source sur À propos et le guide ; fiches/modèles — veille 44-88 kWh, mode sombre nuancé (Purdue), −75/−90 %, HTTP Archive 2024, PUE 1,56/1,45, source EEA/RTE du mix, « Cyber World CleanUp Day », durées liste/détail alignées, règle UE 2023/1670 corrigée (smartphones/tablettes), cibles 5 ans/7 ans, rowSpan du cahier des charges, seuils de la grille sur 78, intro 5 onglets du tableau de bord. Vérifié : `tsc` 0 erreur, build, export, navigateur (en-têtes colorés du guide, zoom au clavier, liens des fiches).
- Contrôle complémentaire du 15/09 : nouvelle passe croisée (3 sous-agents) sur l'exactitude, la pédagogie et les 7 outils. Outils : e-mail ×365 dans le calculateur, « économie possible » corrigée (30 % au lieu de 70 %), arbres 20 kg/an (au lieu de 2,5), voiture 0,17 kg/km, bœuf 7 kg, reconditionné −75 % au lieu de −25 % dans le simulateur, labels e-mail alignés (−1/−2 %), état initial neutre (durée 2 ans), entreprise : scénario de base sans coût de mise en œuvre et « non rentable » au lieu de 0 mois, reconditionné 50 kg partout, audit parc : garde parc vide (plus de NaN) et gains étiquetés « à l'achat », AWS 100 % renouvelable. Faits : 1,6 Mt = production (accueil, FAQ, recyclage : 0,9 Mt collectés), 2→3 ans = « environ un tiers », box 26 kWh Arcep 2026, e-mails 4-35 g, crédit d'impôt retiré, « Guide officiel » → récapitulatif, France 2020/2022 précisée, nucléaire ~67 %, cibles de durée harmonisées (5 ans smartphone/7 ans ordinateur), grille d'audit /78 (26 critères × 3), 50 kg = cycle de vie (dont ~39 kg fabrication), 60 % + 15 % explicités, offline honnête, lien interne /outils en `Link` (bug basePath), glossaire enrichi (scope 1/2/3, effet rebond, WUE, EENM) + lien footer, parcours développeur/entreprise/collectivité diversifiés, page chiffres reliée. Vérifié : `tsc` 0 erreur, build, export, outils testés à l'écran (pas de NaN, 0 % au départ, économie 30 %, labels −1/−2 %).
- Contre-audit des audits le 15/09 : chaque recommandation de `audit-contenu-2026-09.md` (§2 à §11) recoupée avec le code. Corrigés : `developpement` (JS ×4,45 Pereira 2017, GPT-3 552 t = ~120 voitures/an, MobileNet/CDN attribués), ISO 14001/50001 sans chiffres inventés, allégations Scaleway/OVH/Qarnot attribuées à leurs communications, eau laptop 1 500 à 20 000+ L selon méthode, reconditionné −75 à −90 % (ADEME 2022) partout, France 2,5 %/17,2 Mt (2020) et 4,4 %/29,5 Mt (2022), comparateur cloud (PUE ~17 %, Google matching 24/7 2030, données 2024-2026), calculateur (e-mail 4 g, repas bœuf 7 kg, hypothèses SD/hors vidéo affichées), FAQ → annuaire ADEME, ADN/quantique nuancés, datacenters 10 000+ et projection AIE ~945 TWh 2030 + EED >500 kW, textes UE (ESPR, USB-C, Green Claims, Data Act, IA Act), eau/usage chiffres harmonisés (usage 20 %), images restantes en français (cycle de vie 16:9 + 4 vignettes fiches, 39-42 Ko) et recycling-electronics recompressé. Vérifié : `tsc` 0 erreur, build, export.
- Reste soldé le 15/09 : **16 erreurs `tsc`** corrigées (`@types/leaflet` en devDependency, `LabeledSlider` typé `number[]`, `SearchDialog` en `Dispatch<SetStateAction<boolean>>` — 0 erreur restante). **Langage** harmonisé sur 12 fichiers : vouvoiement partout, plus d'injonctions ni d'exclamations professorales (accueil, `comprendre`, `agir`, `mythes`, FAQ, `cas-pratiques`, quiz, simulateur, `developpement`), espace insécable avant `%`, `CO₂e`, guillemets français, PUE/WUE définis, « e-déchets » → « déchets électroniques ». **Reliquats** : lien `ecosystem.eco` en absolu, ancre `/outils#onglet-calculator`, 6 `image:` morts de `[id]` retirés + 2 webp orphelins supprimés, chiffres FAQ/`guide`/`datacenters`/`website-carbon-calculator` sourcés. **Docs** : SW `v1.3.0`, canonical, suivi. Vérifié : `tsc` 0 erreur, build, export.
- Images E le 15/09 (commit `f1e2e6b`) : infographie smartphone régénérée en français (SVG déterministe → webp 98 Ko : 70 kg, 50 kg CO₂, 12 000 L, 15 000 km, 50+ métaux, source ADEME Base Empreinte 2023) après un essai Agnes au texte illisible ; `comparison-chart-new-vs-refurbished-vs-repaire.webp` (charabia, jamais rendu car `sheet-content` n'affiche pas `sheet.image`) supprimé avec sa référence morte. Vérifié : build, export sans l'ancien fichier.
- Finalisation D le 15/09 (commits `91ba803` à `7b6fa85`) : reliquats corrigés (`reglementation` : bloc « Impact mesuré » introuvable retiré, remplacé par bonus 4→25 M€ ADEME 2025 et lave-vaisselle +28 % Univers Habitat 2025 ; `developpement` WebP 65 %→~30 % Google ; `problematiques` 3,4 %/1,8 Gt, 1,5 % AIE, 52,7 % cloud Eurostat, 22,3 % GEM, 5 % réseaux, indice durabilité UE, DeepMind 2016/ARM 2023 sourcés ; `recyclage` 46 %/1,6 Mt/79 % Ecosystem ; `chiffres` 1 an de plus ≈ un tiers + résumé sourcé ; `comprendre` sources ADEME partout, 46 % ; `cas-pratiques` reconditionné 8 kg, 800 kWh sourcé ; `fiches-pratiques` 79 %, sources, GPT-3 552 t, veille calculée ; `mythes` Back Market retiré ; `modeles` années 2024-2027 passées en relatif ; `sobriety-simulator` 285 kg GreenIT EENM 2025 ; `enterprise-simulator` reconditionné −75 % ; `agir`/calculateur 720p harmonisés). Technique : `canonical` par page (26 routes + 8 fiches + 7 modèles, accueil défini par défaut dans le layout racine), PWA précache `/greenit/offline/` + fallback, sitemap `lastModified` 15/09, breadcrumb complété, `ressources` intitulés/tailles corrigés, `sitemap-page` en tokens, emojis nav retirés, `aria-label` contact. Accessibilité : infobulle `SourceTooltip` (source + calcul) sur les chiffres clés (accueil, chiffres, recyclage, problematiques, cas-pratiques, reglementation), contrastes corrigés (héros emerald-700→teal-800, badges, `/50`-`/80`, footer, shared). Vérifié : `tsc`, build, export, tests Firefox et WebKit (8 pages, 0 erreur nouvelle), captures.
- collecte honnête le 14/09 (tâche 1 vague 1) : `chiffres` + `recyclage` marqués « Chiffres illustratifs : exemple de mise en page, pas des données officielles » avec lien officiel ADEME « Que faire de mes objets » (`quefairedemesdechets.ademe.fr`, `target blank` + `noopener`), `15 000+`/`15 000 bornes` remplacés par des formulations neutres sans nouveau total (stat `recyclage` devenue « Carte », service Ecosystem « dans toute la France », fiche `recyclage-mode-emploi` + ressource ADEME cliquable). Vérifié : build ok (42 pages), export sans `15 000` sur ces pages, aucun bouton ajouté.
- mythe stockage cloud le 15/09 (tâche 2 vague 1) : `20 kg` (`mythes`), `15 g` (`faq` + fiche `gestes-quotidiens`), `0,2 kg/Go` (calculateur) harmonisés à `0,24 g CO2e/Go/an` (ADEME, Impact CO2 / Base Empreinte) avec message recentré sur la durée de vie des appareils (~50 kg cycle de vie smartphone), ton sympa sans culpabiliser ; facteur calculateur à `0,00024 kg/Go/an` (poste quasi nul, normal et juste) + hypothèse affichée près des sources. Vérifié : build ok, export contient `0,24 g` sur les 4 pages (chunks JS pour les 3 pages clientes), zéro résidu `15g`/`0,2 kg` cloud.
- Hygiène le 15/09 : `npm audit` 13 vulnérabilités (2 critiques) → **0** (`sharp@0.35.4`, `next@16.3.5` dans la même majeure, validé avec l'utilisateur), scan secrets propre, vérif live (SW servi en `v1.1.0` alors que le dépôt est en `v1.3.0` → déploiement en ligne en retard, à resynchroniser côté hébergement ; pas de `robots.txt` racine, rien de bloqué).
- Accessibilité le 15/09 : respect de `prefers-reduced-motion` (filet CSS global + hook `usePrefersReducedMotion`, minuteurs JS et particule SVG coupés), audit axe-core 10 pages → **0 violation critique/sérieuse** (noms des boutons de frise, contrastes, `select` du quiz, camembert, région scrollable, guide), 7 `h1` dupliqués des modèles corrigés, focus 8/8 (menus de nav), Escape OK, Firefox/WebKit 0 erreur. Puis Lighthouse : 10 curseurs nommés, 2 barres de progression nommées, ordre des titres, 2 contrastes de boutons, marqueurs Leaflet nommés par ville → a11y Lighthouse 96-100.
- Performance le 15/09 : Lighthouse desktop accueil 94→96, outils 96, comprendre 96, chiffres 94→96 ; `jspdf` en import dynamique au clic « Télécharger » (chunk de 440 Ko sorti du chargement d'onglet) — et au passage **export PDF des outils réparé** (`jspdf-autotable` 5.x a supprimé `doc.autoTable`, migré vers `autoTable(doc, options)`, vérifié par téléchargement réel des deux PDF).
- SEO le 15/09 : JSON-LD (`WebSite` + `Organization` sur l'accueil, `BreadcrumbList` sur toutes les pages internes via le fil d'Ariane, `FAQPage` sur `/faq`, `TechArticle` sur les 8 fiches, aucun bloc invalide), OpenGraph + description par page (40/41 pages ont `og:url` = canonical ; reste `/offline/`, noindex), titres et descriptions distincts vérifiés dans l'export.
- Quiz v2 le 15/09 : reprise de session (tous modes, y compris après rechargement ; en Défi le chrono continue de s'écouler et les questions non répondues sont marquées « Non répondu »), dernière session terminée conservée avec bandeau « Derniers résultats » (Revoir/Refaire), révision des erreurs en accordéon (votre réponse, bonne réponse, explication, source) et « Rejouer mes erreurs » en session `review` sans attestation. Stockage local `greenit-quiz-session-v1` versionné (session invalidée si les questions changent), mentions légales complétées, boutons d'impression du quiz repassés en `text-white` (contraste). Vérifié : 12 tests node du stockage, parcours playwright (reprise onglet/reload, rejeu, Défi expiré), axe 0 sur sélection/résultats/erreurs dépliées, Firefox/WebKit 0.

- grosse relecture pessimiste, lecture seule, sans toucher au code. Résultat dans `todo.md` (P0 basePath/PWA + refs mortes).
- docs passées en minuscules : `readme.md`, `about.md`, `todo.md`, `changelog.md` (les autres `agents.md`, `claude.md`, `structure.md`, `features.md`, `readme_dev.md` restent en local, dans le gitignore).
- `readme.md` refait pour github (court, qui je suis, comment lancer).
- nouveau `readme_dev.md` : mon pense-bête perso, avec les pièges basePath + la checklist.
- `about.md` réécrit, plus perso.
- nouveaux `agents.md` + `claude.md` pour bosser avec les IA en local.
- `structure.md` + `features.md` : état réel + ce qui marche pas.
- `.gitignore` : j'ajoute `readme_dev.md`, `agents.md`, `claude.md`, `structure.md`, `features.md` (+ majuscules au cas où) pour rien pousser d'IA-ish sur le repo public.
- rappels : repo en ligne PUBLIC (vérifié avec `gh repo view`), local sur `main`, 7 dossiers `modeles/*` + `guide/` + `mentions-legales/` pas encore commités.
- acté : route `/flux-rss` virée, `placeholder.*` virés, `styles/globals.css` viré.

## 1.1.0 — 04/01/2026

Upgrade & polish :
- PWA de base (manifest, sw, bouton install, page offline) — bon en dev, à revoir sous `/greenit`, voir P0
- carte leaflet / OSM sur recyclage + chiffres, à la place des visuels statiques
- PDF : audit, simulateur entreprise, guide recyclage
- SEO : sitemap dynamique + robots + metadata
- images en webp, fonts optimisées
- docker multi-étapes + nginx pour coolify

Outils en plus :
- quiz avancé (100 questions, 10 cats, 4 modes)
- analyse site web par url
- comparateur cloud (8)
- audit parc (7 types, note A-E)
- simulateur entreprise (ROI 5 ans)

Fix : images accueil, metadata authors, opengraph.

## 1.0.0 — 04/01/2025

Première version qui tient debout :
- accueil + quiz + parcours, comprendre (cycle de vie 5 étapes), chiffres (graphes), cas pratiques, agir (3 guides), datacenters (PUE), recyclage (carte de base), dev, réglementation (AGEC, REEN, DEEE, CSRD), problématiques, perspectives 2040, actus (12), par-ou-commencer, mythes (12), faq (24), 8 fiches, nav + breadcrumb + recherche, dark mode, glossaire 15 termes
- svg animées, équivalences co2, comparateur langages
- webp + lazy + alt, open graph
- Next 16 / React 19, design tokens, mobile-first. Typo : Poppins + Inter (l'ancien changelog disait Geist, c'était une erreur).

## après

- 1.2.0 : basePath/PWA, refs mortes, recherche et boutons livrés les 14-15/09 ; images et contenus assainis le 15/09 ; reste comptes, newsletter, success stories
- 1.3.0 en 2027 : forum, formation, templates, webinaires, espace jeunes

---

Geoffroy — geoffroy.streit@gmail.com — https://hylst.fr/greenit
