# Quiz v2 (reprise + révision des erreurs) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permettre de reprendre une session de quiz après avoir quitté l'onglet ou rechargé la page (tous modes, chrono réel en Défi), conserver la dernière session terminée, revoir ses erreurs et les rejouer.

**Architecture:** Un module pur `lib/quiz-storage.ts` (schéma, validation, localStorage) + des touches ciblées dans `components/quiz-green-it-advanced.tsx` (restauration au montage, sauvegarde par effet, bandeau, mode `review`) + un composant `components/quiz-error-review.tsx`. Aucun refactor du moteur.

**Tech Stack:** Next 16 client component, React 19, Tailwind v4, TypeScript, localStorage. Tests : scripts node (type stripping Node 24) + playwright no-save + `tsc` + build + axe.

## Global Constraints

- **Aucune nouvelle dépendance runtime** ; outils d'audit en `--no-save` (playwright déjà installé ; sinon `npm i --no-save --legacy-peer-deps playwright@1.62.0 @axe-core/playwright`).
- **Aucune modification des 100 questions** ; si elles changent, incrémenter `QUIZ_CONTENT_VERSION`.
- **Pas de refactor du moteur** (score, niveaux, tirage, timer, attestation) : on ne touche qu'aux états, effets, handlers et écrans.
- **Fin de chaque tâche :** `npx tsc --noEmit` → 0 erreur, `npm run build` → succès.
- **localStorage uniquement côté client, lecture dans un `useEffect` de montage** (pas de mismatch d'hydratation). Toujours enveloppé de `try/catch` : sans stockage, le quiz doit fonctionner normalement.
- **Serveur local :** `node C:/Users/Geoffroy/AppData/Local/Temp/opencode/greenit-server.js "<abs>/out" 3006` ; arrêt via `netstat -ano | grep ":3006"` + `taskkill //PID <pid> //F`.
- **NODE_PATH pour scripts temp :** `NODE_PATH="D:\0CODE\AntiGravity\GreenIT\node_modules" node <script>` lancé depuis la racine du repo.
- **Commits :** français, ciblés (`quiz : …`, `docs : …`), jamais `out/`, `node_modules/`, `.next/`.
- **Docs de suivi :** `changelog.md`, `todo.md`, `suivi-audit-2026-09.md` à jour en fin de chantier.
- **Hors périmètre :** historique multi-sessions, backend/compte, partage, modifications de questions, refonte visuelle.

---

### Task 1: Module de stockage `lib/quiz-storage.ts`

**Files:**
- Create: `lib/quiz-storage.ts`
- Test: `C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-storage-test.mjs` (temporaire, non commité)

**Interfaces:**
- Produces: `QuizMode`, `StoredAnswer`, `QuizSession`, `QUIZ_STORAGE_KEY`, `loadQuizSession({ contentVersion, questionCount })`, `saveQuizSession(session)`, `clearQuizSession()`.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-storage-test.mjs` (extension `.mjs` obligatoire : top-level `await`) :
```js
const memory = new Map()
function fakeStorage() {
  return {
    getItem: (k) => (memory.has(k) ? memory.get(k) : null),
    setItem: (k, v) => memory.set(k, String(v)),
    removeItem: (k) => memory.delete(k),
  }
}
globalThis.window = { localStorage: fakeStorage() }

const assert = (label, cond) => {
  console.log(cond ? "OK  " : "FAIL", label)
  if (!cond) process.exitCode = 1
}

const { QUIZ_STORAGE_KEY, saveQuizSession, loadQuizSession, clearQuizSession } = await import(
  "file:///D:/0CODE/AntiGravity/GreenIT/lib/quiz-storage.ts"
)

const valid = {
  schemaVersion: 1,
  contentVersion: 1,
  mode: "discovery",
  category: null,
  questionIds: [0, 5, 9],
  currentIndex: 1,
  answers: [
    { sessionIndex: 0, selectedAnswer: 2, correct: true },
    { sessionIndex: 1, selectedAnswer: -1, correct: false },
  ],
  score: 15,
  playerName: "Test",
  finished: false,
  finishedAt: null,
  endsAt: null,
}

// round-trip
saveQuizSession(valid)
let loaded = loadQuizSession({ contentVersion: 1, questionCount: 100 })
assert("round-trip complet", loaded && loaded.score === 15 && loaded.questionIds.length === 3 && loaded.mode === "discovery")
assert("round-trip réponses", loaded && loaded.answers[1].selectedAnswer === -1 && loaded.answers[0].correct === true)

// JSON corrompu -> null + nettoyage
window.localStorage.setItem(QUIZ_STORAGE_KEY, "{pas du json")
loaded = loadQuizSession({ contentVersion: 1, questionCount: 100 })
assert("JSON corrompu -> null", loaded === null)
assert("JSON corrompu -> clé nettoyée", window.localStorage.getItem(QUIZ_STORAGE_KEY) === null)

// version de contenu différente -> null
saveQuizSession(valid)
loaded = loadQuizSession({ contentVersion: 2, questionCount: 100 })
assert("contentVersion différente -> null", loaded === null)

// indice hors bornes -> null
saveQuizSession({ ...valid, questionIds: [0, 200] })
loaded = loadQuizSession({ contentVersion: 1, questionCount: 100 })
assert("indice hors bornes -> null", loaded === null)

// answers malformées -> null
saveQuizSession({ ...valid, answers: [{ sessionIndex: 0, selectedAnswer: 1, correct: "oui" }] })
loaded = loadQuizSession({ contentVersion: 1, questionCount: 100 })
assert("answer malformée -> null", loaded === null)

// currentIndex hors bornes -> null
saveQuizSession({ ...valid, currentIndex: 42 })
loaded = loadQuizSession({ contentVersion: 1, questionCount: 100 })
assert("currentIndex hors bornes -> null", loaded === null)

// mode inconnu -> null
saveQuizSession({ ...valid, mode: "triche" })
loaded = loadQuizSession({ contentVersion: 1, questionCount: 100 })
assert("mode inconnu -> null", loaded === null)

// stockage absent -> pas d'exception
delete globalThis.window
assert("sans window : save sans exception", (() => { try { saveQuizSession(valid); return true } catch { return false } })())
assert("sans window : load renvoie null", loadQuizSession({ contentVersion: 1, questionCount: 100 }) === null)
assert("sans window : clear sans exception", (() => { try { clearQuizSession(); return true } catch { return false } })())

console.log(process.exitCode ? "ÉCHEC" : "TOUS LES TESTS PASSENT")
```

- [ ] **Step 2: Lancer le test (échec attendu)**

```bash
node C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-storage-test.mjs
```
Attendu : échec d'import (`ERR_MODULE_NOT_FOUND` sur `lib/quiz-storage.ts`).
Si Node refuse le TypeScript : repli documenté — `npx tsc lib/quiz-storage.ts --outDir C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-js --target es2022 --module es2022 --skipLibCheck` puis remplacer l'import par le `.js` compilé (le test doit alors passer pareil).

- [ ] **Step 3: Créer `lib/quiz-storage.ts`**

```ts
export type QuizMode = "discovery" | "full" | "category" | "challenge" | "review"

export interface StoredAnswer {
  sessionIndex: number
  selectedAnswer: number
  correct: boolean
}

export interface QuizSession {
  schemaVersion: 1
  contentVersion: number
  mode: QuizMode
  category: string | null
  questionIds: number[]
  currentIndex: number
  answers: StoredAnswer[]
  score: number
  playerName: string
  finished: boolean
  finishedAt: string | null
  endsAt: number | null
}

export const QUIZ_STORAGE_KEY = "greenit-quiz-session-v1"

const MODES: QuizMode[] = ["discovery", "full", "category", "challenge", "review"]

export function clearQuizSession() {
  try {
    window.localStorage.removeItem(QUIZ_STORAGE_KEY)
  } catch {
    // stockage indisponible : le quiz continue sans persistance
  }
}

export function saveQuizSession(session: QuizSession) {
  try {
    window.localStorage.setItem(QUIZ_STORAGE_KEY, JSON.stringify(session))
  } catch {
    // stockage indisponible ou plein : le quiz continue sans persistance
  }
}

export function loadQuizSession(expected: { contentVersion: number; questionCount: number }): QuizSession | null {
  let raw: string | null = null
  try {
    raw = window.localStorage.getItem(QUIZ_STORAGE_KEY)
  } catch {
    return null
  }
  if (!raw) return null

  let parsed: unknown = null
  try {
    parsed = JSON.parse(raw)
  } catch {
    clearQuizSession()
    return null
  }

  const session = parsed as Partial<QuizSession> | null
  const questionIds = Array.isArray(session?.questionIds) ? (session?.questionIds as number[]) : null
  const answers = Array.isArray(session?.answers) ? session?.answers : null
  const valid =
    !!session &&
    session.schemaVersion === 1 &&
    session.contentVersion === expected.contentVersion &&
    typeof session.mode === "string" &&
    MODES.includes(session.mode as QuizMode) &&
    !!questionIds &&
    questionIds.length > 0 &&
    questionIds.every((id) => Number.isInteger(id) && id >= 0 && id < expected.questionCount) &&
    Number.isInteger(session.currentIndex) &&
    (session.currentIndex as number) >= 0 &&
    (session.currentIndex as number) < questionIds.length &&
    !!answers &&
    answers.every(
      (a) =>
        a &&
        Number.isInteger(a.sessionIndex) &&
        a.sessionIndex >= 0 &&
        a.sessionIndex < questionIds.length &&
        Number.isInteger(a.selectedAnswer) &&
        a.selectedAnswer >= -1 &&
        typeof a.correct === "boolean",
    ) &&
    typeof session.score === "number" &&
    Number.isFinite(session.score) &&
    session.score >= 0 &&
    typeof session.playerName === "string" &&
    typeof session.finished === "boolean" &&
    (session.finishedAt === null || typeof session.finishedAt === "string") &&
    (session.endsAt === null || typeof session.endsAt === "number")

  if (!valid) {
    clearQuizSession()
    return null
  }

  return session as QuizSession
}
```

- [ ] **Step 4: Lancer le test (succès attendu)**

```bash
node C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-storage-test.mjs
```
Attendu : tous les `OK`, `TOUS LES TESTS PASSENT`.

- [ ] **Step 5: tsc + build + commit**

```bash
npx tsc --noEmit && npm run build 2>&1 | tail -2
git add lib/quiz-storage.ts
git commit -m "quiz : module de stockage local des sessions"
```

---

### Task 2: Reprise de session dans le composant

**Files:**
- Modify: `components/quiz-green-it-advanced.tsx` (imports, types, états, effets, handlers, `resetQuiz`)

**Interfaces:**
- Consumes: `lib/quiz-storage.ts` (Task 1).
- Produces: `QUIZ_CONTENT_VERSION`, `QuizQuestion` exporté, restauration complète (mode, questions, index, score, réponses, chrono), sauvegarde par effet, `finishedAt`.

- [ ] **Step 1: Imports et type exporté**

En tête de `components/quiz-green-it-advanced.tsx`, après les imports existants :
```tsx
import {
  loadQuizSession,
  saveQuizSession,
  clearQuizSession,
  type QuizMode,
  type QuizSession,
  type StoredAnswer,
} from "@/lib/quiz-storage"
```
Passer `interface QuizQuestion` en `export interface QuizQuestion` (ligne 22).

- [ ] **Step 2: Supprimer le type local et ajouter la version de contenu**

Supprimer la ligne `type QuizMode = "discovery" | "full" | "category" | "challenge"` (elle est désormais importée).
Juste avant `export function QuizGreenITAdvanced()`, ajouter :
```tsx
// À incrémenter si les questions changent (invalide proprement les sessions en cours)
const QUIZ_CONTENT_VERSION = 1

const questionIndex = (question: QuizQuestion) => ALL_QUIZ_QUESTIONS.indexOf(question)
```

- [ ] **Step 3: États**

Remplacer le bloc d'états (lignes ~1456-1466) par :
```tsx
  const [mode, setMode] = useState<QuizMode | null>(null)
  const [selectedCategory, setSelectedCategory] = useState<string | null>(null)
  const [activeQuestions, setActiveQuestions] = useState<QuizQuestion[]>([])
  const [currentQuestionIndex, setCurrentQuestionIndex] = useState(0)
  const [selectedAnswer, setSelectedAnswer] = useState<number | null>(null)
  const [showExplanation, setShowExplanation] = useState(false)
  const [score, setScore] = useState(0)
  const [answers, setAnswers] = useState<StoredAnswer[]>([])
  const [isFinished, setIsFinished] = useState(false)
  const [finishedAt, setFinishedAt] = useState<string | null>(null)
  const [timeLeft, setTimeLeft] = useState(0)
  const [endsAt, setEndsAt] = useState<number | null>(null)
  const [playerName, setPlayerName] = useState("")
  const [restored, setRestored] = useState(false)
```

- [ ] **Step 4: Restauration au montage + sauvegarde par effet**

Juste après `const categories = ...`, ajouter le helper de consolidation et la restauration :
```tsx
  const withUnanswered = (base: StoredAnswer[], questions: QuizQuestion[]) => {
    const answered = new Set(base.map((a) => a.sessionIndex))
    const missing = questions
      .map((_, i) => i)
      .filter((i) => !answered.has(i))
      .map((i) => ({ sessionIndex: i, selectedAnswer: -1, correct: false }))
    return missing.length ? [...base, ...missing] : base
  }

  const restoreSession = (session: QuizSession) => {
    const questions = session.questionIds.map((id) => ALL_QUIZ_QUESTIONS[id]).filter(Boolean)
    if (questions.length !== session.questionIds.length) {
      clearQuizSession()
      return
    }
    setMode(session.mode)
    setSelectedCategory(session.category)
    setActiveQuestions(questions)
    setCurrentQuestionIndex(session.currentIndex)
    setSelectedAnswer(null)
    setShowExplanation(false)
    setScore(session.score)
    setAnswers(session.answers)
    setPlayerName(session.playerName)
    setFinishedAt(session.finishedAt)
    setEndsAt(session.endsAt)
    if (session.mode === "challenge" && session.endsAt) {
      const remaining = Math.max(0, Math.ceil((session.endsAt - Date.now()) / 1000))
      setTimeLeft(remaining)
      if (session.finished || remaining <= 0) {
        setIsFinished(true)
        if (!session.finished) {
          setAnswers(withUnanswered(session.answers, questions))
          setFinishedAt(new Date().toISOString())
        }
      } else {
        setIsFinished(false)
      }
    } else {
      setTimeLeft(0)
      setIsFinished(session.finished)
    }
  }

  useEffect(() => {
    const stored = loadQuizSession({ contentVersion: QUIZ_CONTENT_VERSION, questionCount: ALL_QUIZ_QUESTIONS.length })
    if (stored && !stored.finished) restoreSession(stored)
    setRestored(true)
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [])

  useEffect(() => {
    if (!restored || !mode || activeQuestions.length === 0) return
    saveQuizSession({
      schemaVersion: 1,
      contentVersion: QUIZ_CONTENT_VERSION,
      mode,
      category: selectedCategory,
      questionIds: activeQuestions.map(questionIndex),
      currentIndex: currentQuestionIndex,
      answers,
      score,
      playerName,
      finished: isFinished,
      finishedAt,
      endsAt,
    })
  }, [restored, mode, selectedCategory, activeQuestions, currentQuestionIndex, answers, score, playerName, isFinished, finishedAt, endsAt])
```
Note : un session terminée au chargement sera branchée en Task 4 (bandeau). Ici elle est simplement ignorée à la reprise.

- [ ] **Step 5: Timer du Défi**

Remplacer l'effet du timer par :
```tsx
  useEffect(() => {
    if (mode === "challenge" && timeLeft > 0 && !isFinished) {
      const timer = setTimeout(() => setTimeLeft(timeLeft - 1), 1000)
      return () => clearTimeout(timer)
    }
    if (timeLeft === 0 && mode === "challenge" && !isFinished && endsAt) {
      setAnswers(withUnanswered(answers, activeQuestions))
      setIsFinished(true)
      setFinishedAt(new Date().toISOString())
    }
  }, [timeLeft, isFinished, mode, endsAt, answers, activeQuestions])
```

- [ ] **Step 6: `startQuiz`, `handleAnswer`, `handleFinish`, `resetQuiz`**

Dans `startQuiz`, après `setIsFinished(false)` :
```tsx
    setFinishedAt(null)
    setAnswers([])
    setSelectedAnswer(null)
    setShowExplanation(false)
```
et dans chaque branche de mode :
- `discovery`/`full`/`category` : ajouter `setEndsAt(null)` ;
- `challenge` : remplacer `setTimeLeft(20 * 60)` par :
```tsx
      setTimeLeft(20 * 60)
      setEndsAt(Date.now() + 20 * 60 * 1000)
```

Remplacer `handleAnswer` par :
```tsx
  const handleAnswer = (answerIndex: number) => {
    if (selectedAnswer !== null) return

    setSelectedAnswer(answerIndex)
    setShowExplanation(true)

    const currentQuestion = activeQuestions[currentQuestionIndex]
    const correct = answerIndex === currentQuestion.correctAnswer
    if (correct) setScore(score + currentQuestion.points)
    setAnswers([...answers, { sessionIndex: currentQuestionIndex, selectedAnswer: answerIndex, correct }])
  }
```

Remplacer `handleFinish` par :
```tsx
  const handleFinish = () => {
    setAnswers(withUnanswered(answers, activeQuestions))
    setIsFinished(true)
    setFinishedAt(new Date().toISOString())
  }
```

Dans `resetQuiz`, remplacer `setAnsweredQuestions([])` par `setAnswers([])` et ajouter :
```tsx
    setFinishedAt(null)
    setEndsAt(null)
    clearQuizSession()
```

- [ ] **Step 7: Écran de résultats — remplacer `answeredQuestions`**

Lignes ~1707 et ~1712, remplacer `answeredQuestions.length` par `answers.length` (2 occurrences). Supprimer toute autre référence à `answeredQuestions`.

- [ ] **Step 8: tsc + build + test manuel rapide**

```bash
npx tsc --noEmit && npm run build 2>&1 | tail -2
```
Puis serveur + vérification playwright rapide (script ci-dessous) :
```js
// C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-resume-test.js
const { chromium } = require("playwright")
;(async () => {
  const browser = await chromium.launch()
  const context = await browser.newContext()
  const page = await context.newPage()
  await page.goto("http://localhost:3006/greenit/outils/", { waitUntil: "networkidle" })
  await page.locator("#onglet-quiz").click()
  await page.getByRole("button", { name: /mode découverte/i }).click()
  await page.getByRole("radio").first().click()
  await page.getByRole("button", { name: /question suivante/i }).click()
  await page.getByRole("radio").first().click()
  await page.getByRole("button", { name: /question suivante/i }).click()
  const q3 = await page.getByText("Question 3 / 10").count()
  await page.locator("#onglet-calculator").click()
  await page.waitForTimeout(300)
  await page.locator("#onglet-quiz").click()
  await page.waitForTimeout(500)
  const resumed = await page.getByText("Question 3 / 10").count()
  await page.reload({ waitUntil: "networkidle" })
  await page.locator("#onglet-quiz").click()
  await page.waitForTimeout(500)
  const resumedReload = await page.getByText("Question 3 / 10").count()
  const stored = await page.evaluate(() => JSON.parse(localStorage.getItem("greenit-quiz-session-v1") || "null"))
  console.log("q3:", q3, "| reprise onglet:", resumed, "| reprise reload:", resumedReload, "| score stocké:", stored?.score, "| answers:", stored?.answers?.length)
  await browser.close()
  process.exit(q3 === 1 && resumed === 1 && resumedReload === 1 && stored?.answers?.length === 2 ? 0 : 1)
})()
```
Lancer : `NODE_PATH="D:\0CODE\AntiGravity\GreenIT\node_modules" node C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-resume-test.js`
Attendu : `q3: 1 | reprise onglet: 1 | reprise reload: 1 | score stocké: … | answers: 2`, exit 0.

- [ ] **Step 9: Commit**

```bash
git add components/quiz-green-it-advanced.tsx
git commit -m "quiz : reprise de session (tous modes, chrono reel)"
```

---

### Task 3: Révision et rejeu des erreurs

**Files:**
- Create: `components/quiz-error-review.tsx`
- Modify: `components/quiz-green-it-advanced.tsx` (import, écran de résultats, `startErrorReview`)

**Interfaces:**
- Consumes: `QuizQuestion` (Task 2), `StoredAnswer` (Task 1).
- Produces: `ErrorReviewItem`, composant `ErrorReview`, mode `review`.

- [ ] **Step 1: Créer `components/quiz-error-review.tsx`**

```tsx
"use client"

import { useState } from "react"
import { ChevronDown, CheckCircle2, XCircle } from "lucide-react"
import type { QuizQuestion } from "@/components/quiz-green-it-advanced"

export interface ErrorReviewItem {
  question: QuizQuestion
  selectedAnswer: number
  sessionIndex: number
}

export function ErrorReview({ items }: { items: ErrorReviewItem[] }) {
  const [open, setOpen] = useState<number[]>([])

  if (items.length === 0) {
    return (
      <div className="rounded-lg border border-emerald-200 bg-emerald-50 p-4 text-emerald-800 dark:border-emerald-800 dark:bg-emerald-900/20 dark:text-emerald-300">
        Sans faute, bravo !
      </div>
    )
  }

  return (
    <div className="space-y-2">
      {items.map(({ question, selectedAnswer, sessionIndex }, index) => {
        const expanded = open.includes(index)
        return (
          <div
            key={`${sessionIndex}-${index}`}
            className="overflow-hidden rounded-lg border border-slate-200 dark:border-slate-600"
          >
            <button
              onClick={() => setOpen(expanded ? open.filter((i) => i !== index) : [...open, index])}
              aria-expanded={expanded}
              className="flex w-full items-center justify-between gap-3 p-3 text-left hover:bg-slate-50 dark:hover:bg-slate-700/50"
            >
              <span className="text-sm font-medium text-slate-900 dark:text-gray-100">{question.question}</span>
              <ChevronDown
                className={`h-4 w-4 shrink-0 transition-transform ${expanded ? "rotate-180" : ""}`}
                aria-hidden="true"
              />
            </button>
            {expanded && (
              <div className="space-y-2 border-t border-slate-200 p-3 text-sm dark:border-slate-600">
                <p className="flex items-start gap-2 text-red-700 dark:text-red-400">
                  <XCircle className="mt-0.5 h-4 w-4 shrink-0" aria-hidden="true" />
                  {selectedAnswer === -1 ? "Non répondu" : `Votre réponse : ${question.options[selectedAnswer]}`}
                </p>
                <p className="flex items-start gap-2 text-green-700 dark:text-green-400">
                  <CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0" aria-hidden="true" />
                  Bonne réponse : {question.options[question.correctAnswer]}
                </p>
                <p className="text-slate-600 dark:text-gray-300">{question.explanation}</p>
                <p className="text-xs text-slate-500 dark:text-gray-400">Source : {question.source}</p>
              </div>
            )}
          </div>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 2: Importer et calculer les erreurs dans le composant**

Dans `components/quiz-green-it-advanced.tsx`, ajouter l'import :
```tsx
import { ErrorReview, type ErrorReviewItem } from "@/components/quiz-error-review"
```
Dans l'écran de résultats (`if (isFinished)`), après le calcul de `percentage` :
```tsx
    const errorItems: ErrorReviewItem[] = answers
      .filter((answer) => !answer.correct)
      .map((answer) => ({
        question: activeQuestions[answer.sessionIndex],
        selectedAnswer: answer.selectedAnswer,
        sessionIndex: answer.sessionIndex,
      }))
      .filter((item) => item.question)
```

- [ ] **Step 3: Intégrer la section dans l'écran de résultats**

Dans l'écran de résultats, juste avant le bloc « Prochaines étapes », insérer :
```tsx
          <div className="space-y-3">
            <div className="flex flex-wrap items-center justify-between gap-2">
              <h4 className="font-semibold text-slate-900 dark:text-gray-100">
                Revoir mes erreurs ({errorItems.length})
              </h4>
              {errorItems.length > 0 && (
                <Button variant="outline" size="sm" onClick={startErrorReview}>
                  <RotateCcw className="mr-2 h-4 w-4" />
                  Rejouer mes erreurs ({errorItems.length})
                </Button>
              )}
            </div>
            <ErrorReview items={errorItems} />
          </div>
```

- [ ] **Step 4: Attestation exclue du mode `review`**

Toujours dans l'écran de résultats :
- remplacer `{percentage >= 60 && (` par `{percentage >= 60 && mode !== "review" && (` (bloc attestation) ;
- remplacer la condition du bouton d'impression `{percentage >= 60 ? (` par `{percentage >= 60 && mode !== "review" ? (`.

- [ ] **Step 5: `startErrorReview`**

Après `handleFinish`, ajouter :
```tsx
  const startErrorReview = () => {
    const errorIds = answers
      .filter((answer) => !answer.correct)
      .map((answer) => questionIndex(activeQuestions[answer.sessionIndex]))
      .filter((id) => id >= 0)
    if (errorIds.length === 0) return

    setMode("review")
    setSelectedCategory(null)
    setActiveQuestions(errorIds.map((id) => ALL_QUIZ_QUESTIONS[id]))
    setCurrentQuestionIndex(0)
    setSelectedAnswer(null)
    setShowExplanation(false)
    setScore(0)
    setAnswers([])
    setIsFinished(false)
    setFinishedAt(null)
    setTimeLeft(0)
    setEndsAt(null)
    setPlayerName("")
  }
```

- [ ] **Step 6: tsc + build + contrôle manuel**

```bash
npx tsc --noEmit && npm run build 2>&1 | tail -2
```
Contrôle manuel (navigateur sur le serveur local, onglet Quiz) : terminer une session Découverte en jouant les réponses (il y aura des erreurs sur 10 questions), puis vérifier :
- « Revoir mes erreurs (N) » avec N > 0 et aucune attestation en mode `review` ;
- ouverture d'une erreur (clic ou clavier) → « Votre réponse » rouge, « Bonne réponse » verte, explication, source ;
- « Rejouer mes erreurs (N) » → « Question 1 / N » et `localStorage.getItem("greenit-quiz-session-v1")` → `"mode":"review"`.
Le test déterministe (session terminée injectée) se fait en Task 4, Step 4.

- [ ] **Step 7: Commit**

```bash
git add components/quiz-error-review.tsx components/quiz-green-it-advanced.tsx
git commit -m "quiz : revision et rejeu des erreurs"
```

---

### Task 4: Bandeau « derniers résultats »

**Files:**
- Modify: `components/quiz-green-it-advanced.tsx` (écran de sélection + état `lastFinished`)

**Interfaces:**
- Consumes: `QuizSession`, `restoreSession`, `resetQuiz` (Tasks 2-3).
- Produces: bandeau avec « Revoir mes erreurs » / « Refaire un quiz ».

- [ ] **Step 1: État + chargement**

Ajouter l'état :
```tsx
  const [lastFinished, setLastFinished] = useState<QuizSession | null>(null)
```
Dans l'effet de montage (Task 2), remplacer :
```tsx
    if (stored && !stored.finished) restoreSession(stored)
```
par :
```tsx
    if (stored && !stored.finished) restoreSession(stored)
    if (stored && stored.finished) setLastFinished(stored)
```

Dans `resetQuiz`, ajouter `setLastFinished(null)`.

- [ ] **Step 2: Bandeau sur l'écran de sélection**

Dans `if (!mode) {`, avant le `return`, calculer :
```tsx
    const lastFinishedQuestions = lastFinished
      ? lastFinished.questionIds.map((id) => ALL_QUIZ_QUESTIONS[id]).filter(Boolean)
      : []
    const lastFinishedMaxScore = lastFinishedQuestions.reduce((sum, q) => sum + q.points, 0)
    const lastFinishedPercentage =
      lastFinished && lastFinishedMaxScore > 0 ? Math.round((lastFinished.score / lastFinishedMaxScore) * 100) : 0
    const lastFinishedErrors = lastFinished ? lastFinished.answers.filter((a) => !a.correct).length : 0
```
Puis, au début du `<CardContent className="space-y-4">` :
```tsx
          {lastFinished && (
            <div className="flex flex-col gap-3 rounded-xl border-2 border-emerald-200 bg-emerald-50 p-4 dark:border-emerald-800 dark:bg-emerald-900/20 md:flex-row md:items-center md:justify-between">
              <div>
                <p className="font-semibold text-emerald-900 dark:text-emerald-200">
                  Derniers résultats : {lastFinished.score} / {lastFinishedMaxScore} ({lastFinishedPercentage} %)
                </p>
                <p className="text-sm text-emerald-800 dark:text-emerald-300">
                  {lastFinishedErrors > 0
                    ? `${lastFinishedErrors} erreur${lastFinishedErrors > 1 ? "s" : ""} à revoir`
                    : "Sans faute, bravo !"}
                </p>
              </div>
              <div className="flex gap-2">
                <Button variant="outline" size="sm" onClick={() => restoreSession(lastFinished)}>
                  Revoir mes erreurs
                </Button>
                <Button variant="ghost" size="sm" onClick={resetQuiz}>
                  Refaire un quiz
                </Button>
              </div>
            </div>
          )}
```

- [ ] **Step 3: tsc + build**

```bash
npx tsc --noEmit && npm run build 2>&1 | tail -2
```

- [ ] **Step 4: Test playwright complet**

Étendre `C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-review-test.js` :
```js
const { chromium } = require("playwright")
;(async () => {
  const browser = await chromium.launch()
  const context = await browser.newContext()
  const page = await context.newPage()
  await page.goto("http://localhost:3006/greenit/outils/", { waitUntil: "networkidle" })
  await page.evaluate(() => {
    localStorage.setItem(
      "greenit-quiz-session-v1",
      JSON.stringify({
        schemaVersion: 1,
        contentVersion: 1,
        mode: "discovery",
        category: null,
        questionIds: [0, 1, 2],
        currentIndex: 2,
        answers: [
          { sessionIndex: 0, selectedAnswer: 0, correct: false },
          { sessionIndex: 1, selectedAnswer: 1, correct: true },
          { sessionIndex: 2, selectedAnswer: -1, correct: false },
        ],
        score: 10,
        playerName: "",
        finished: true,
        finishedAt: new Date().toISOString(),
        endsAt: null,
      })
    )
  })
  await page.reload({ waitUntil: "networkidle" })
  await page.locator("#onglet-quiz").click()
  await page.waitForTimeout(500)

  const banner = await page.getByText(/derniers résultats/i).count()
  console.log("bandeau:", banner)

  await page.getByRole("button", { name: /revoir mes erreurs/i }).click()
  await page.waitForTimeout(500)
  const results = await page.getByText(/résultats du quiz/i).count()
  console.log("écran résultats:", results)

  await page.locator("div.overflow-hidden.rounded-lg button[aria-expanded]").first().click()
  await page.waitForTimeout(300)
  const expanded = await page.getByText("Non répondu").count()
  console.log("détail erreur (non répondu):", expanded)

  const replay = page.getByRole("button", { name: /rejouer mes erreurs \(2\)/i })
  await replay.click()
  await page.waitForTimeout(500)
  const q1 = await page.getByText("Question 1 / 2").count()
  const mode = await page.evaluate(() => JSON.parse(localStorage.getItem("greenit-quiz-session-v1") || "null")?.mode)
  console.log("rejeu:", q1, "| mode stocké:", mode)

  await browser.close()
  process.exit(banner === 1 && results === 1 && expanded === 1 && goodAnswer === 1 && q1 === 1 && mode === "review" ? 0 : 1)
})()
```
Lancer avec NODE_PATH. Attendu : `bandeau: 1 | écran résultats: 1 | détail erreur: 1 | rejeu: 1 | mode stocké: review`, exit 0.
Note : le clic d'expansion (`aria-expanded`) peut viser le bouton de la question ; si le sélecteur par texte échoue, cliquer le premier `button[aria-expanded]` de la section (adapter le script, pas le produit).

Complément mode Défi expiré :
```js
  // dans le même script, avant fermeture :
  await page.evaluate(() => {
    const s = JSON.parse(localStorage.getItem("greenit-quiz-session-v1"))
    localStorage.setItem(
      "greenit-quiz-session-v1",
      JSON.stringify({ ...s, mode: "challenge", questionIds: [0, 1], answers: [], currentIndex: 0, score: 0, finished: false, endsAt: Date.now() - 5000 })
    )
  })
  await page.reload({ waitUntil: "networkidle" })
  await page.locator("#onglet-quiz").click()
  await page.waitForTimeout(500)
  const expired = await page.getByText(/résultats du quiz/i).count()
  await page.locator("div.overflow-hidden.rounded-lg button[aria-expanded]").first().click()
  await page.waitForTimeout(300)
  const expiredUnanswered = await page.getByText("Non répondu").count()
  console.log("défi expiré -> résultats:", expired, "| non répondu:", expiredUnanswered)
```
Attendu : `expired: 1`.

- [ ] **Step 5: Commit**

```bash
git add components/quiz-green-it-advanced.tsx
git commit -m "quiz : bandeau derniers resultats"
```

---

### Task 5: Mentions légales, vérifications finales, docs, push

**Files:**
- Modify: `app/mentions-legales/page.tsx`
- Modify: `changelog.md`, `todo.md`, `suivi-audit-2026-09.md`

**Interfaces:**
- Consumes: tout le chantier.
- Produces: livraison.

- [ ] **Step 1: Mentions légales**

Lire la section « données » de `app/mentions-legales/page.tsx` et ajouter dans la liste existante (même style) :
```tsx
              <li>
                Le quiz conserve dans votre navigateur (localStorage) votre session en cours et vos derniers
                résultats, pour vous permettre de reprendre et de revoir vos erreurs. Ces données ne quittent
                jamais votre appareil.
              </li>
```

- [ ] **Step 2: Batterie complète**

```bash
npx tsc --noEmit && npm run build 2>&1 | tail -2
node C:/Users/Geoffroy/AppData/Local/Temp/opencode/h1-scan.js
node C:/Users/Geoffroy/AppData/Local/Temp/opencode/jsonld-scan.js
NODE_PATH="D:\0CODE\AntiGravity\GreenIT\node_modules" node C:/Users/Geoffroy/AppData/Local/Temp/opencode/a11y-axe.js
NODE_PATH="D:\0CODE\AntiGravity\GreenIT\node_modules" node C:/Users/Geoffroy/AppData/Local/Temp/opencode/quiz-review-test.js
NODE_PATH="D:\0CODE\AntiGravity\GreenIT\node_modules" node C:/Users/Geoffroy/AppData/Local/Temp/opencode/a11y-browsers.js
```
Attendu : tsc 0 ; build OK ; h1 0 hors norme ; JSON-LD 0 invalide ; axe 0 critique/sérieuse (l'écran quiz inclus) ; test quiz exit 0 ; Firefox/WebKit 0 erreur hors bruit `_rsc`.
Note : pour que l'axe couvre l'écran de résultats et la liste d'erreurs, ouvrir une session terminée (comme dans le test) avant de lancer l'analyse ; si le script axe générique ne le fait pas, le vérifier manuellement au clavier (accordéons `aria-expanded`, focus visible).

- [ ] **Step 3: Docs**

- `changelog.md` : une entrée « Quiz v2 le 15/09 » (reprise tous modes, chrono réel en Défi, dernière session terminée, révision et rejeu des erreurs, stockage local mentionné dans les mentions légales).
- `todo.md` : cocher la ligne quiz ; reste : tests lecteur d'écran, historique de scores non retenu.
- `suivi-audit-2026-09.md` : vérifications datées + ce qui reste.

- [ ] **Step 4: Scan secrets + commit + push**

```bash
git add app/mentions-legales/page.tsx changelog.md todo.md suivi-audit-2026-09.md
git commit -m "docs : quiz v2 verifie"
git push origin main
```
Récapitulatif final : comportements vérifiés (reprise onglet/reload, chrono Défi, bandeau, erreurs, rejeu), limites (contenu modifié → bump `QUIZ_CONTENT_VERSION`, pas d'historique).

---

## Notes d'exécution

- Le composant est monté à l'ouverture de l'onglet Quiz de `/outils` : la reprise n'a lieu qu'à ce moment-là (voulu).
- Ne jamais lire `localStorage` au rendu initial ; uniquement dans les effets.
- Si un `npm install` retire playwright/axe : relancer `npm i --no-save --legacy-peer-deps playwright@1.62.0 @axe-core/playwright` avant les tâches navigateur.
- En cas d'écart entre ce plan et le code réel (ligne déplacée, texte changé), s'arrêter, lire le fichier, appliquer la correction minimale — pas de refactor opportuniste.
